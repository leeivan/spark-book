# 8.6 小结

本章梳理�?Spark 结构化机器学习流程中常见的特征工程工具。对于文本特征，可以使用 TF-IDF、Word2Vec 和各�?Vectorizer；对于类别和数值特征，可以使用编码器、缩放器、离散化器与缺失值处理器；对于特征裁剪与降维，则可以使用 VectorSlicer、PCA、Chi-Square Selector 等组件。更重要的是，这些能力在 Spark 4.x 中应当放到统一�?`spark.ml + DataFrame + Pipeline` 主线下理解，而不是孤立地逐个记忆�?


