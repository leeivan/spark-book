# 4.4 案例分析

表格
4�?展示�?`/data/sfpd.csv` 文件中的字段说明和样例数据。该数据集来自某市警察局�?2013 �?1 月到 2015 �?7 月之间的报案记录�?

表格 4�? 案例分析数据说明

| 列名          | 描述      | 值例                             |
| ----------- | ------- | ------------------------------ |
| IncidentNum | 报案编号    | 150561637                      |
| Category    | 报案类别    | NON-CRIMINAL                   |
| Descript    | 报案描述    | FOUND\_PROPERTY                |
| DayOfWeek   | 报案的星�?  | Sunday                         |
| Date        | 报案发生的日�?| 6/28/15                        |
| Time        | 报案发生的时�?| 23:50                          |
| PdDistrict  | 报案区域    | TARAVAL                        |
| Resolution  | 解决方式    | NONE                           |
| Address     | 地址      | 1300\_Block\_of\_LA\_PLAYA\_ST |
| X           | 位置X坐标   | \-122.5091348                  |
| Y           | 位置Y坐标   | 37.76119777                    |
| PdID        | 部门ID    | 15056163704013                 |

这个案例的目标是：用 DataFrame 定义结构化数据，使用 Spark 提供的结构化 API 完成探索分析，并顺带理解临时视图、SQL 查询�?UDF 的基本配合方式�?

从工程角度看，这个案例更像一条完整的结构化处理链路：先把原始文件组织成带 Schema 的数据集，再通过 DataFrame �?SQL 完成筛选、分组、排序和统计，最后在确有需要时补一个简�?UDF。读者阅读这一节时，重点不必放在“把每个 API 都记住”，而应放在结构化处理的组织顺序上�?

在团队协作里，这类案例还有一个很常见的演化路径：分析初期，大家往往先通过临时视图和几�?SQL 快速确认业务口径；一旦查询逻辑稳定下来，就再把它整理成可复用的 DataFrame 链式代码，放进正式应用、Notebook 模板或测试用例里。也正因为如此，本节会同时保�?DataFrame API �?SQL 两种入口，让读者看到它们如何围绕同一份结构化数据协同工作�?

### 4.4.1 创建DataFrame

现实项目里，创建 DataFrame 的首选方式通常是直接使�?`spark.read.csv/json/parquet/...` 从数据源读取，因为这样最符合 Spark 4.x 的结构化主线。本节之所以仍然保留“从RDD构建DataFrame”的过程，主要是为了帮助读者理�?Schema、Row、案例类和临时视图之间的关系，同时兼容少量遗留输入流程�?

从现�?RDD 创建 DataFrame，常见有两条路径：一种是已知对象结构时，通过案例类和反射推断 Schema；另一种是显式构�?`StructType` �?`Row`，在运行时把 Schema 应用�?RDD 上。前者更简洁，后者更灵活。下面先看第一种方式，它可以拆�?4 个动作：读入原始 RDD、定义案例类、把每一行映射成案例类对象、再隐式转换�?DataFrame 并注册视图�?

下面先看具体实例�?

  - 创建RDD

这里先把 CSV 文本读成一个最原始�?`RDD[Array[String]]`。这一步还没有 Schema，主要是为了给后面的案例类映射做准备�?

```scala
scala> val sfpdRDD =
sc.textFile("/data/sfpd.csv").map(inc=>inc.split(","))
sfpdRDD: org.apache.spark.rdd.RDD[Array[String]] =
MapPartitionsRDD[2] at map at <console>:24
```

代码 4�?1
  - 定义案例�?

```scala
scala> case class Incidents(incidentnum:String,
category:String,description:String, dayofweek:String, date:String,
time:String,pddistrict:String, resolution:String, address:String,
X:Float,Y:Float, pdid:String)
defined class Incidents
```

代码 4�?2
  - 转换RDD

接着把每一行数组映射成 `Incidents` 对象。做完这一步后，数据已经从“位置式字符串数组”变成了“带字段语义的对�?RDD”�?

```scala
scala> val
sfpdCase=sfpdRDD.map(inc=>Incidents(inc(0),inc(1),inc(2),inc(3),inc(4),inc(5),inc(6),inc(7),inc(8),inc(9).toFloat,inc(10).toFloat,
inc(11)))
sfpdCase: org.apache.spark.rdd.RDD[Incidents] = MapPartitionsRDD[3]
at map at <console>:28
```

代码 4�?3
  - 创建数据�?

然后通过 `toDF()` �?`sfpdCase` 隐式转换�?DataFrame。到了这一步，后续就可以使用过滤、聚合、排序、SQL 查询等结构化操作�?

```scala
scala> val sfpdDF=sfpdCase.toDF()
sfpdDF: org.apache.spark.sql.DataFrame = [incidentnum: string,
category: string ... 10 more fields]
```

代码 4�?4
  - 注册临时视图

�?DataFrame 注册为临时视图后，就可以同时对比 DataFrame API �?SQL 两种写法。这也是本节案例的重点之一：它们只是表达方式不同，最终都落在同一套结构化执行引擎上�?

```scala
scala> sfpdDF.createOrReplaceTempView("sfpd")
```

代码 4�?5
通过上面的步骤，现在就可以使用SQL语言查询视图 `sfpd` 了。这展示的是“案例类 + 反射推断Schema”的路径。接下来再看第二种方式：当字段结构需要运行时决定，或者原始文本需要先手工解析�?`Row` 时，可以显式构造Schema后再创建DataFrame�?

�?）从原始RDD创建包含Row对象的RDD

�?）创建由StructType表示的数据结构，该结构与创建的RDD中的Row对象结构相匹配�?

�?）通过SparkSession提供的createDataFrame 方法将数据结构应用于RDD中的Row对象上�?

下面继续使用 `/data/sfpd.csv`，但这次不保留全部字段，而是只抽�?`IncidentNum`、`Date` �?`PdDistrict` 三列来演示“显式构�?Schema”的路径。这样做的目的不是为了追求最短代码，而是为了让读者看�?`Row`、`StructField`、`StructType` �?`createDataFrame()` 之间是如何配合的�?

  - 首先需要导入必要的类�?

```scala
scala> import org.apache.spark.sql.types._
import org.apache.spark.sql.types._
```

代码 4�?7
  - 创建Row RDD

在这一步里，先把文本文件拆成数组，再挑出我们真正关心的三个字段，最后把它们组装�?`Row` 对象�?

```scala
scala> import org.apache.spark.sql.Row
import org.apache.spark.sql.Row
scala> val rowRDD =
sc.textFile("/data/sfpd.csv").map(x=>x.split(",")).map(p=>Row(p(0),p(4),p(6)))
rowRDD: org.apache.spark.rdd.RDD[org.apache.spark.sql.Row] =
MapPartitionsRDD[146] at map at <console>:29
scala> rowRDD.first
res66: org.apache.spark.sql.Row =
[150599321,7/9/15,CENTRAL]
```

代码 4�?8
  - 创建模式

StructType对象定义了数据结构，需要一个StructField对象的数组。StructType接受的参数为(fields：Array
\[StructField\])；StructField采用以下参数(name: String, dataType: DataType,
nullable: Boolean = true)�?

```scala
scala> val testsch =
StructType(Array(StructField("IncNum",StringType,nullable =
true),StructField("Date",StringType,nullable =
true),StructField("District",StringType,nullable = true)))
testsch: org.apache.spark.sql.types.StructType =
StructType(StructField(IncNum,StringType,true),
StructField(Date,StringType,true),
StructField(District,StringType,true))
```

代码 4�?9
在这一步里，我们显式声明了一个名�?`testsch` 的结构，其中包含 `IncNum`、`Date` �?`District` 三个字段。这里都使用 `StringType`，并允许为空；真实项目里则应根据源数据质量和字段语义选择更准确的类型�?

  - 创建DataFrame

```scala
scala> val testDF = spark.createDataFrame(rowRDD,testsch)
testDF: org.apache.spark.sql.DataFrame = [IncNum: string, Date: string
... 1 more field]
scala> testDF.show
+---------+------+--------+
|   IncNum|  Date|District|
+---------+------+--------+
|150599321|7/9/15| CENTRAL|
|156168837|7/9/15| CENTRAL|
|150599224|7/9/15| CENTRAL|
|150599230|7/9/15| CENTRAL|
|150599309|7/9/15| CENTRAL|
+---------+------+--------+
only showing top 20 rows
```

代码 4�?0
这里的关键不�?API 记忆，而是理解 `RowRDD + StructType + createDataFrame()` 这条组合关系：一旦把 Schema 显式补齐，Spark 就能重新回到结构化处理主线。创建出 DataFrame 后，同样可以注册临时视图并继续用 SQL 查询�?

```scala
scala> testDF.createOrReplaceTempView("test")
scala> val incs = sql("SELECT * FROM test")
20/03/20 01:22:49 WARN ObjectStore: Failed to get database global_temp,
returning NoSuchObjectException
incs: org.apache.spark.sql.DataFrame = [IncNum: string, Date: string
... 1 more field]
scala> incs.show(2)
+---------+------+--------+
|   IncNum|  Date|District|
+---------+------+--------+
|150599321|7/9/15| CENTRAL|
|156168837|7/9/15| CENTRAL|
+---------+------+--------+
only showing top 2 rows
```

代码 4�?1
### 4.4.2 操作DataFrame

接下来进入真正的分析阶段。这里故意保�?DataFrame API �?SQL 两种写法，是为了让读者看到：在结构化处理里，很多问题都可以先写成链式操作，再按需要切换成 SQL 视图查询。我们先从几个很典型的问题开始：

  - 5个发生报案最多的区域是什么？

  - 5个发生报案最多的街区是什么？

  - �?0个报案解决方式是什么？

  - �?0个报案类别是什么？

这些问题表面上是“找前几名”“做分组统计”，本质上对应的是结构化处理中最常见的一组动作：`groupBy`、`count`、`sort`、`limit`、`selectExpr` 以及 SQL 查询。先回答其中一个代表性问题，就足以把这条处理链看清楚�?

  - 5个发生报案最多的街区是什么？这个问题可以拆成 6 个小动作�?

<!-- end list -->

  - > 1.先按地址分组，得到一�?`RelationalGroupedDataset`�?

```scala
scala> val incByAdd = sfpdDF.groupBy("address")
incByAdd: org.apache.spark.sql.RelationalGroupedDataset =
RelationalGroupedDataset: [grouping expressions: [address: string],
value: [incidentnum: string, category: string ... 10 more fields],
type: GroupBy]
```

代码 4�?2
  - > 2.在分组结果上计算每个地址的报案数�?

```scala
scala> val numAdd = incByAdd.count
numAdd: org.apache.spark.sql.DataFrame = [address: string, count:
bigint]
```

代码 4�?3
  - > 3.把结果按报案数降序排列�?

```scala
scala> val numAddDesc = numAdd.sort($"count".desc)
numAddDesc: org.apache.spark.sql.Dataset[org.apache.spark.sql.Row] =
[address: string, count: bigint]
```

代码 4�?4
  - > 4.显示前五名�?

```scala
scala> numAddDesc.show(5)
+--------------------+-----+
| address|count|
+--------------------+-----+
|800_Block_of_BRYA...|10852|
|800_Block_of_MARK...| 3671|
|1000_Block_of_POT...| 2027|
|2000_Block_of_MIS...| 1585|
| 16TH_ST/MISSION_ST| 1512|
+--------------------+-----+
only showing top 5 rows
top5Add: Unit = ()
```

代码 4�?5
  - > 5.如果只是一次性查询，也可以把前面的链式调用压成一条语句�?

```scala
scala> sfpdDF.groupBy("address").count.sort($"count".desc).show(5)
+--------------------+-----+
| address|count|
+--------------------+-----+
|800_Block_of_BRYA...|10852|
|800_Block_of_MARK...| 3671|
|1000_Block_of_POT...| 2027|
|2000_Block_of_MIS...| 1585|
| 16TH_ST/MISSION_ST| 1512|
+--------------------+-----+
only showing top 5 rows
```

代码 4�?6
  - > 6.同一个问题也可以直接写成 SQL�?

```scala
scala> sql("SELECT address, count(incidentnum) AS inccount FROM sfpd
GROUP BY address ORDER BY inccount DESC LIMIT 5").show
+--------------------+--------+
| address|inccount|
+--------------------+--------+
|800_Block_of_BRYA...| 10852|
|800_Block_of_MARK...| 3671|
|1000_Block_of_POT...| 2027|
|2000_Block_of_MIS...| 1585|
| 16TH_ST/MISSION_ST| 1512|
+--------------------+--------+
```

代码 4�?7
这个例子说明了结构化处理的一条典型路径：先组�?DataFrame，再通过分组、聚合和排序回答业务问题。如果要把结果继续交给下游系统，还可以直接使用写操作把结果保存到文件或表中，例如�?

```scala
scala> val top5Add = numAddDesc.limit(5)
top5Add: org.apache.spark.sql.Dataset[org.apache.spark.sql.Row] =
[address: string, count: bigint]
scala>
top5Add.write.format("json").mode("overwrite").save("/data/top5Add.json")
```

代码 4�?8
这里�?`top5Add` �?JSON 格式写出，并使用 `overwrite` 模式覆盖旧结果。在真实项目里，更常见的输出目标往往�?Parquet、湖仓表或下游数仓表，而不是只写一�?JSON 文件；不过这个例子足以说明结构化结果可以自然地衔接到写出阶段�?

### 4.4.3 按年份组�?

数据集中的日期是 `dd/mm/yy` 形式的字符串。为了按年份聚合，首先要把年份从字符串中提取出来。这里保留一个最�?UDF 示例，主要是为了说明 Spark SQL 里“如何定义并复用自定义逻辑”；但从工程实践看，如果内置字符串函数已经足够，通常应优先使用内置函数而不�?UDF�?

Spark SQL 和其他查询引擎一样支持用户定义函数，而且既可以在 DataFrame API 中调用，也可以注册后�?SQL 里复用。下面先定义一个最小的年份提取函数�?

�?）定义用户定义函�?

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
val getStr = udf((s:String)=>{
val lastS = s.substring(s.lastIndexOf('/')+1)
lastS
})
// Exiting paste mode, now interpreting.
getStr: org.apache.spark.sql.expressions.UserDefinedFunction =
UserDefinedFunction(<function1>,StringType,Some(List(StringType)))
```

代码 4�?9
�?）在DataFrame操作中使用用户定义函�?

```scala
scala> sfpdDF.groupBy(getStr(sfpdDF("date"))).count.show
+---------+------+
|UDF(date)| count|
+---------+------+
| 15| 80760|
| 13|152830|
| 14|150185|
+---------+------+
```

代码 4�?0
上面的例子说明了 DataFrame API 中直接调�?UDF 的方式。如果希望在 SQL 语句里复用同样的逻辑，还可以先把它注册成命名函数，再�?`SELECT` 里引用�?

�?）定义和注册用户定义函数
```scala
spark.sqlContext.udf.register("getStr", (s: String) => {
  val strAfter = s.substring(s.lastIndexOf('/') + 1)
  strAfter
})
```

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
spark.sqlContext.udf.register("getStr", (s:String)=>{
val strAfter = s.substring(s.lastIndexOf('/')+1)
strAfter
})
// Exiting paste mode, now interpreting.
res4: org.apache.spark.sql.expressions.UserDefinedFunction =
UserDefinedFunction(<function1>,StringType,Some(List(StringType)))
```

代码 4�?1
�?）在SQL语句中使用UDF

```scala
scala> sql("SELECT getStr(date), count(incidentnum) AS countByYear FROM
sfpd GROUP BY getStr(date) ORDER BY countByYear DESC LIMIT 5").show
20/03/20 11:55:51 WARN ObjectStore: Failed to get database global_temp,
returning NoSuchObjectException
+----------------+-----------+
|UDF:getStr(date)|countByYear|
+----------------+-----------+
| 13| 152830|
| 14| 150185|
| 15| 80760|
+----------------+-----------+
```

代码 4�?2


