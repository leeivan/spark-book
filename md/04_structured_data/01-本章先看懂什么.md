# 4.1 本章先看懂什么
- `SparkSession + DataFrame` 为什么是 Spark 4.x 结构化处理的默认入口。
- DataFrame / Dataset / RDD 的使用边界，以及什么时候真的需要回退到 RDD。
- Schema 的价值：它不仅描述字段，还决定了优化器能做什么。
- 常见结构化操作的思维方式：过滤、聚合、窗口、Join 以及 `explain()` 背后的执行计划。
