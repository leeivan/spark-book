# 11.8 小结

本章沿着“先理解执行，再观察现象，最后做有证据的优化”这条线，介绍了 Spark 作业的执行模型、Shuffle 与统一内存机制、Web UI 的主要观察入口，以及序列化、缓存、并行度、广播变量和数据局部性这些常见调优抓手。读完本章后，读者应能先判断瓶颈更可能落在 Stage 切分、数据倾斜、序列化、缓存策略、GC 还是资源配置，再决定下一步应该改代码、改数据布局，还是改运行参数。
