# 5.1 本章先看懂什么

流处理为什么要面对无界数据、触发机制和持续输出；事件时间、水位线、状态与 checkpoint 如何共同决定结果正确性；在 Spark 4.x 中为什么应优先使用 Structured Streaming，而把 DStream 视为历史兼容材料。

## 一个最小例子

需求：实时统计每分钟错误日志数。
从 Kafka 读取日志流；解析日志级别和时间；按分钟窗口聚合 `ERROR` 数量；写入控制台或告警系统。

先跑通“读流 -> 聚合 -> 写出”，再逐步加状态与容错。
