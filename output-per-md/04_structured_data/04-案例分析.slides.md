<!-- generated by doc-to-ppt-talk-track -->

## 4.4 案例分析

- 表格 4‑6展示了 /data/sfpd.csv 文件中的字段说明和样例数据
- 表格 4‑6 案例分析数据说明
- 这个案例的目标是：用 DataFrame 定义结构化数据，使用 Spark 提供的结构化 API 完成探索分析，并顺带理解临时视图、SQL
- 在确有需要时补一个简单 UDF

::: notes
表格 4‑6展示了 /data/sfpd.csv 文件中的字段说明和样例数据。该数据集来自某市警察局在 2013 年 1 月到 2015 年 7 月之间的报案记录。 接下来会依次看 案例分析、创建DataFrame，以及 操作DataFrame，后面的实现和案例都会沿着这条线继续展开。
:::

## 4.4 案例分析：学习路线

- 案例分析
- 创建DataFrame
- 操作DataFrame
- 按年份组合

::: notes
这一页先把 4.4 案例分析 的主线串起来，重点包括 案例分析、创建DataFrame、操作DataFrame，以及 按年份组合。先知道每一步要解决什么问题，后面的细节就更容易跟住。
:::

## 4.4 案例分析（1/2）

- 表格 4‑6展示了 /data/sfpd.csv 文件中的字段说明和样例数据
- 表格 4‑6 案例分析数据说明
- 这个案例的目标是：用 DataFrame 定义结构化数据，使用 Spark 提供的结构化 API 完成探索分析，并顺带理解临时视图、SQL
- 在确有需要时补一个简单 UDF

::: notes
这一页会把 表格 4‑6展示了 /data/sfpd.csv 文件中的字段说明和样例数据、表格 4‑6 案例分析数据说明 和 用 DataFrame 定义结构化数据 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 4.4 案例分析（2/2）

- 这类案例还有一个很常见的演化路径
- 一旦查询逻辑稳定下来

::: notes
这一页先看 这类案例还有一个很常见的演化路径，再看 一旦查询逻辑稳定下来。把这两步连起来讲清楚，页面内容和后面的实现流程就能对上。
:::

## 案例分析：表格 4‑6 案例分析数据说明

- 先辨认表头含义，再看不同项之间的差异

| 列名          | 描述      | 值例                             |
| ----------- | ------- | ------------------------------ |
| IncidentNum | 报案编号    | 150561637                      |
| Category    | 报案类别    | NON-CRIMINAL                   |
| Descript    | 报案描述    | FOUND\_PROPERTY                |
| DayOfWeek   | 报案的星期   | Sunday                         |
| Date        | 报案发生的日期 | 6/28/15                        |
| Time        | 报案发生的时间 | 23:50                          |
| PdDistrict  | 报案区域    | TARAVAL                        |
| Resolution  | 解决方式    | NONE                           |
| Address     | 地址      | 1300\_Block\_of\_LA\_PLAYA\_ST |
| X           | 位置X坐标   | \-122.5091348                  |
| Y           | 位置Y坐标   | 37.76119777                    |
| PdID        | 部门ID    | 15056163704013                 |

::: notes
先把表头对应的对象和维度说清楚，再指出哪些差异最大，以及这些差异能支持什么判断。
:::

## 4.4.1 创建DataFrame（1/2）

- 创建 DataFrame 的首选方式通常是直接使用
- 从现有 RDD 创建 DataFrame，常见有两条路径：一种是已知对象结构时，通过案例类和反射推断 Schema
- 它可以拆成 4 个动作
- 具体实例

::: notes
这一页会把 创建 DataFrame 的首选方式通常是直接使用、从现有 RDD 创建 DataFrame 和 它可以拆成 4 个动作 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 4.4.1 创建DataFrame（2/2）

- 创建RDD
- 这一步还没有 Schema，主要是为了给后面的案例类映射做准备
- 代码 4‑31
- 定义案例类

::: notes
这一页会把 创建RDD、这一步还没有 Schema，主要是为了给后面的案例类映射做准备 和 代码 4‑31 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 4.4.1 创建DataFrame：代码片段（1/7）

- 把原始数据从存储读入 Spark
- 先对照代码理解 定义案例类 的执行步骤

片段 1
```scala
scala> val sfpdRDD =
sc.textFile("/data/sfpd.csv").map(inc=>inc.split(","))
sfpdRDD: org.apache.spark.rdd.RDD[Array[String]] =
MapPartitionsRDD[2] at map at \<console\>:24
```

片段 2
```scala
scala> case class Incidents(incidentnum:String,
category:String,description:String, dayofweek:String, date:String,
time:String,pddistrict:String, resolution:String, address:String,
X:Float,Y:Float, pdid:String)
defined class Incidents
```

::: notes
这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.1 创建DataFrame：代码片段（2/7）

- 用 map 把输入转换成后续要处理的结构
- 后续就可以使用过滤

片段 1
```scala
scala> val
sfpdCase=sfpdRDD.map(inc=>Incidents(inc(0),inc(1),inc(2),inc(3),inc(4),inc(5),inc(6),inc(7),inc(8),inc(9).toFloat,inc(10).toFloat,
inc(11)))
sfpdCase: org.apache.spark.rdd.RDD[Incidents] = MapPartitionsRDD[3]
at map at \<console\>:28
```

片段 2
```scala
scala> val sfpdDF=sfpdCase.toDF()
sfpdDF: org.apache.spark.sql.DataFrame = [incidentnum: string,
category: string ... 10 more fields]
```

::: notes
map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.1 创建DataFrame：代码片段（3/7）

- 先对照代码理解 把 DataFrame 注册为临时视图后
- 先对照代码理解 首先需要导入必要的类。 的执行步骤

片段 1
```scala
scala> sfpdDF.createOrReplaceTempView("sfpd")
```

片段 2
```scala
scala> import org.apache.spark.sql.types._
import org.apache.spark.sql.types._
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.1 创建DataFrame：代码片段（4/7）

- 把原始数据从存储读入 Spark

```scala
scala> import org.apache.spark.sql.Row
import org.apache.spark.sql.Row
scala> val rowRDD =
sc.textFile("/data/sfpd.csv").map(x=>x.split(",")).map(p=>Row(p(0),p(4),p(6)))
rowRDD: org.apache.spark.rdd.RDD[org.apache.spark.sql.Row] =
MapPartitionsRDD[146] at map at \<console\>:29
scala> rowRDD.first
res66: org.apache.spark.sql.Row =
[150599321,7/9/15,CENTRAL]
```

::: notes
这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。
:::

## 4.4.1 创建DataFrame：代码片段（5/7）

- 先对照代码理解 StructType对象定义了数据结构

```scala
scala> val testsch =
StructType(Array(StructField("IncNum",StringType,nullable =
true),StructField("Date",StringType,nullable =
true),StructField("District",StringType,nullable = true)))
testsch: org.apache.spark.sql.types.StructType =
StructType(StructField(IncNum,StringType,true),
StructField(Date,StringType,true),
StructField(District,StringType,true))
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.1 创建DataFrame：代码片段（6/7）

- 先对照代码理解 创建DataFrame 的执行步骤

```scala
scala> val testDF = spark.createDataFrame(rowRDD,testsch)
testDF: org.apache.spark.sql.DataFrame = [IncNum: string, Date: string
... 1 more field]
scala> testDF.show
+---------+------+--------+
|   IncNum|  Date|District|
+---------+------+--------+
|150599321|7/9/15| CENTRAL|
|156168837|7/9/15| CENTRAL|
|150599224|7/9/15| CENTRAL|
|150599230|7/9/15| CENTRAL|
// ...
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.1 创建DataFrame：代码片段（7/7）

- 通过 action 触发前面的惰性计算真正执行

```scala
scala> testDF.createOrReplaceTempView("test")
scala> val incs = sql("SELECT * FROM test")
20/03/20 01:22:49 WARN ObjectStore: Failed to get database global_temp,
returning NoSuchObjectException
incs: org.apache.spark.sql.DataFrame = [IncNum: string, Date: string
... 1 more field]
scala> incs.show(2)
+---------+------+--------+
|   IncNum|  Date|District|
+---------+------+--------+
|150599321|7/9/15| CENTRAL|
// ...
```

::: notes
前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 4.4.2 操作DataFrame（1/2）

- 很多问题都可以先写成链式操作
- 5个发生报案最多的区域是什么？
- 5个发生报案最多的街区是什么？
- 前10个报案解决方式是什么？

::: notes
这一页会把 很多问题都可以先写成链式操作、5个发生报案最多的区域是什么？ 和 5个发生报案最多的街区是什么？ 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 4.4.2 操作DataFrame（2/2）

- 前10个报案类别是什么？
- 这些问题表面上是“找前几名”“做分组统计”
- 先按地址分组，得到一个 RelationalGroupedDataset

::: notes
本质上对应的是结构化处理中最常见的一组动作：groupBy、count、sort、limit、selectExpr 以及 SQL 查询。先回答其中一个代表性问题，就足以把这条处理链看清楚。
:::

## 4.4.2 操作DataFrame：代码片段（1/7）

- 按业务键把相关记录汇到同一组

```scala
scala> val incByAdd = sfpdDF.groupBy("address")
incByAdd: org.apache.spark.sql.RelationalGroupedDataset =
RelationalGroupedDataset: [grouping expressions: [address: string],
value: [incidentnum: string, category: string ... 10 more fields],
type: GroupBy]
```

::: notes
分组这一步决定了结果的统计粒度。只要把按什么键聚说清楚，听众就能知道最后的指标到底是按用户、订单还是时间窗口汇总出来的。
:::

## 4.4.2 操作DataFrame：代码片段（2/7）

- 先对照代码理解 在分组结果上计算每个地址的报案数。 的执行步骤

```scala
scala> val numAdd = incByAdd.count
numAdd: org.apache.spark.sql.DataFrame = [address: string, count:
bigint]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.2 操作DataFrame：代码片段（3/7）

- 先对照代码理解 把结果按报案数降序排列。 的执行步骤

```scala
scala> val numAddDesc = numAdd.sort($"count".desc)
numAddDesc: org.apache.spark.sql.Dataset[org.apache.spark.sql.Row] =
[address: string, count: bigint]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.2 操作DataFrame：代码片段（4/7）

- 通过 action 触发前面的惰性计算真正执行

```scala
scala> numAddDesc.show(5)
+--------------------+-----+
| address|count|
+--------------------+-----+
|800_Block_of_BRYA...|10852|
|800_Block_of_MARK...| 3671|
|1000_Block_of_POT...| 2027|
|2000_Block_of_MIS...| 1585|
| 16TH_ST/MISSION_ST| 1512|
+--------------------+-----+
only showing top 5 rows
top5Add: Unit = ()
```

::: notes
前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 4.4.2 操作DataFrame：代码片段（5/7）

- 按业务键把相关记录汇到同一组

```scala
scala> sfpdDF.groupBy("address").count.sort($"count".desc).show(5)
+--------------------+-----+
| address|count|
+--------------------+-----+
|800_Block_of_BRYA...|10852|
|800_Block_of_MARK...| 3671|
|1000_Block_of_POT...| 2027|
|2000_Block_of_MIS...| 1585|
| 16TH_ST/MISSION_ST| 1512|
+--------------------+-----+
only showing top 5 rows
```

::: notes
分组这一步决定了结果的统计粒度。只要把按什么键聚说清楚，听众就能知道最后的指标到底是按用户、订单还是时间窗口汇总出来的。
:::

## 4.4.2 操作DataFrame：代码片段（6/7）

- 在分组后做聚合计算得到指标结果
- 按业务键把相关记录汇到同一组

```scala
scala> sql("SELECT address, count(incidentnum) AS inccount FROM sfpd
GROUP BY address ORDER BY inccount DESC LIMIT 5").show
+--------------------+--------+
| address|inccount|
+--------------------+--------+
|800_Block_of_BRYA...| 10852|
|800_Block_of_MARK...| 3671|
|1000_Block_of_POT...| 2027|
|2000_Block_of_MIS...| 1585|
| 16TH_ST/MISSION_ST| 1512|
+--------------------+--------+
```

::: notes
这里才真正把多条记录压成业务指标。讲解时要说明聚合函数在累加什么、合并什么，以及最终留下来的结果代表什么含义。

分组这一步决定了结果的统计粒度。只要把按什么键聚说清楚，听众就能知道最后的指标到底是按用户、订单还是时间窗口汇总出来的。
:::

## 4.4.2 操作DataFrame：代码片段（7/7）

- 最后把结果写回外部存储或结果表

```scala
scala> val top5Add = numAddDesc.limit(5)
top5Add: org.apache.spark.sql.Dataset[org.apache.spark.sql.Row] =
[address: string, count: bigint]
scala>
top5Add.write.format("json").mode("overwrite").save("/data/top5Add.json")
```

::: notes
这一步把计算结果落到外部系统，供下游查询、报表或下一轮任务继续使用。讲的时候要说明输出位置和结果的消费方式。
:::

## 4.4.3 按年份组合（1/2）

- 数据集中的日期是 dd/mm/yy 形式的字符串
- 从工程实践看，如果内置字符串函数已经足够，通常应优先使用内置函数而不是 UDF
- Spark SQL 和其他查询引擎一样支持用户定义函数
- （1）定义用户定义函数

::: notes
这一页会把 数据集中的日期是 dd/mm/yy 形式的字符串、从工程实践看，如果内置字符串函数已经足够，通常应优先使用内置函数而不是 UDF 和 Spark SQL 和其他查询引擎一样支持用户定义函数 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 4.4.3 按年份组合（2/2）

- 代码 4‑50 上面的例子说明了 DataFrame API 中直接调用
- （1）定义和注册用户定义函数
- 代码 4‑51 （2）在SQL语句中使用UDF
- 代码 4‑52

::: notes
UDF 的方式。如果希望在 SQL 语句里复用同样的逻辑，还可以先把它注册成命名函数，再在 SELECT 里引用。
:::

## 4.4.3 按年份组合：代码片段（1/5）

- 先对照代码理解 （1）定义用户定义函数 的执行步骤

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
val getStr = udf((s:String)=>{
val lastS = s.substring(s.lastIndexOf('/')+1)
lastS
})
// Exiting paste mode, now interpreting.
getStr: org.apache.spark.sql.expressions.UserDefinedFunction =
UserDefinedFunction(\<function1\>,StringType,Some(List(StringType)))
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.3 按年份组合：代码片段（2/5）

- 按业务键把相关记录汇到同一组

```scala
scala> sfpdDF.groupBy(getStr(sfpdDF("date"))).count.show
+---------+------+
|UDF(date)| count|
+---------+------+
| 15| 80760|
| 13|152830|
| 14|150185|
+---------+------+
```

::: notes
分组这一步决定了结果的统计粒度。只要把按什么键聚说清楚，听众就能知道最后的指标到底是按用户、订单还是时间窗口汇总出来的。
:::

## 4.4.3 按年份组合：代码片段（3/5）

- 先对照代码理解 （1）定义和注册用户定义函数 的执行步骤

```scala
spark.sqlContext.udf.register("getStr", (s: String) => {
  val strAfter = s.substring(s.lastIndexOf('/') + 1)
  strAfter
})
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.3 按年份组合：代码片段（4/5）

- 先对照代码理解 （1）定义和注册用户定义函数 的执行步骤

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
spark.sqlContext.udf.register("getStr", (s:String)=>{
val strAfter = s.substring(s.lastIndexOf('/')+1)
strAfter
})
// Exiting paste mode, now interpreting.
res4: org.apache.spark.sql.expressions.UserDefinedFunction =
UserDefinedFunction(\<function1\>,StringType,Some(List(StringType)))
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 4.4.3 按年份组合：代码片段（5/5）

- 在分组后做聚合计算得到指标结果

```scala
scala> sql("SELECT getStr(date), count(incidentnum) AS countByYear FROM
sfpd GROUP BY getStr(date) ORDER BY countByYear DESC LIMIT 5").show
20/03/20 11:55:51 WARN ObjectStore: Failed to get database global_temp,
returning NoSuchObjectException
+----------------+-----------+
|UDF:getStr(date)|countByYear|
+----------------+-----------+
| 13| 152830|
| 14| 150185|
| 15| 80760|
+----------------+-----------+
```

::: notes
这里才真正把多条记录压成业务指标。讲解时要说明聚合函数在累加什么、合并什么，以及最终留下来的结果代表什么含义。
:::

