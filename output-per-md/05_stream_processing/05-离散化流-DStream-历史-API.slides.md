<!-- generated by doc-to-ppt-talk-track -->

## 5.5 离散化流（DStream，历史 API）

- 兼容性说明：用于理解 Spark 微批模型与维护历史系统
- 请将 5.8 和 5.9 视为本章主线
- Spark Streaming 是 Spark 早期的流处理扩展
- 从今天的工程视角看，DStream 更适合回答两个问题：一是 Spark 的微批模型是怎样形成的

::: notes
兼容性说明：本节介绍 DStream / Spark Streaming，用于理解 Spark 微批模型与维护历史系统。在 Spark 4.x 的新项目中，不应再以 DStream 作为首选实现模板。请将 5.8 和 5.9 视为本章主线。 接下来会依次看 离散化流（DStream，历史 API）、一个例子，以及 迁移对照：DStream 到 Structured Streaming，后面的实现和案例都会沿着这条线继续展开。
:::

## 5.5 离散化流（DStream，历史 API）（1/2）

- 离散化流（DStream，历史 API）
- 一个例子
- 迁移对照：DStream 到 Structured Streaming
- StreamingContext

::: notes
这一页先看前半段主线，重点包括 离散化流（DStream，历史 API）、一个例子、迁移对照：DStream 到 Structured Streaming，以及 StreamingContext。先把前面的入口和处理顺序放稳，后面再继续往下展开。
:::

## 5.5 离散化流（DStream，历史 API）（2/2）

- 输入流

::: notes
这一页补上后半段主线，重点包括 输入流。把这几步和前面的内容接起来，整节课的推进顺序就完整了。
:::

## 5.5 离散化流（DStream，历史 API）（1/2）

- 兼容性说明：用于理解 Spark 微批模型与维护历史系统
- 请将 5.8 和 5.9 视为本章主线
- Spark Streaming 是 Spark 早期的流处理扩展
- 从今天的工程视角看，DStream 更适合回答两个问题：一是 Spark 的微批模型是怎样形成的

::: notes
本节介绍 DStream / Spark Streaming，用于理解 Spark 微批模型与维护历史系统。在 Spark 4.x 的新项目中，不应再以 DStream 作为首选实现模板。

其核心抽象是 DStream。它通过把连续数据流切成一系列小批次，再把每个批次表示为底层 RDD 来执行计算。
:::

## 5.5 离散化流（DStream，历史 API）（2/2）

- 二是旧系统代码为何大量围绕 RDD 和批次间隔编写
- 是要处理的数据必须来自某个外部动态数据源
- 图例 5‑5 Spark数据流总体框架
- 是基于NoSql的数据存储，如HBase等用于存储处理的数据

::: notes
理解这些之后，再回头看 Structured Streaming，会更容易理解“无界表 + 增量执行”的统一模型。

Spark Streaming总体架构通常由以下组件构成（图例 5‑5）。首先是要处理的数据必须来自某个外部动态数据源，例如传感器、移动应用程序、Web客户端、服务器日志等等，这个数据通过消息机制传送给数据采集系统，如Kafka、Flume等，递送或沉积在文件系统中。

然后是流处理过程，获得的数据由Spark Streaming系统进行处理。
:::

## 5.5 离散化流（DStream，历史 API）：图示（1）

![](../../md/media/05_stream_processing/media/image5.jpeg){width=72%}

::: notes
Spark Streaming总体架构通常由以下组件构成（图例 5‑5）。首先是要处理的数据必须来自某个外部动态数据源，例如传感器、移动应用程序、Web客户端、服务器日志等等，这个数据通过消息机制传送给数据采集系统，如Kafka、Flume等，递送或沉积在文件系统中。
:::

## 5.5 离散化流（DStream，历史 API）：图示（2）

![火流](../../md/media/05_stream_processing/media/image6.png){width=72%}

::: notes
Spark Streaming的内部工作原理如下（图例 5‑6）。Spark数据流接收实时输入数据流并将数据分成批，然后由Spark引擎进行处理，以批量生成最终的结果流。
:::

## 5.5.1 一个例子（1/2）

- 在详细介绍Spark数据流程序之前，来看一个简单的Spark数据流程序
- 代码 5‑1
- 这段代码展示的是最经典的 DStream 版 NetworkWordCount
- 需要特别记住的是：程序仍然只是在定义计算链路，真正开始处理数据还要等 ssc.start() 被调用

::: notes
这个程序通过Spark Streaming的TCP套接字接口侦听NetCat发生的数据，统计接收到的文本数据中的字数，这个代码的主程序为。

整体链路其实很简单：先用 SparkConf 和 StreamingContext 建立一个本地流式应用，local[2] 表示至少给接收数据和处理数据各留一个执行线程，批次间隔设为 10 秒。

再用 ssc.socketTextStream() 从 TCP 套接字持续接收文本行，把每一行拆成单词，映射成 (word, 1) 形式的键值对，并在每个微批上用 reduceByKey() 统计词频。最后通过 wordCounts.print() 输出结果。
:::

## 5.5.1 一个例子（2/2）

- 在虚拟实验环境中，上面的应用已经完成编译和打包
- 代码 5‑2
- 使用Docker exec 命令进入到容器中打开另一终端界面
- 代码 5‑3

::: notes
这一页会把 在虚拟实验环境中，上面的应用已经完成编译和打包、代码 5‑2 和 使用Docker exec 命令进入到容器中打开另一终端界面 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 5.5.1 一个例子：代码片段（1/4）

- 用 flatMap 把一条记录展开成多个元素
- 用 map 把输入转换成后续要处理的结构

```scala
import org.apache.spark.SparkConf
import org.apache.spark.streaming.{Seconds, StreamingContext}
object NetworkWordCount {
  def main(args: Array[String]): Unit = {
    if (args.length < 2) {
      System.err.println("Usage: NetworkWordCount \<hostname\> \<port\>")
      System.exit(1)
    }
    val sparkConf = new SparkConf()
      .setAppName("NetworkWordCount")
      .setMaster("local[2]")
// ...
```

::: notes
flatMap 的关键不是做了遍历，而是把一对多的展开放进计算链路。很多词频统计、日志拆词和明细展开都依赖这一步。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 5.5.1 一个例子：代码片段（2/4）

- 先对照代码理解 在虚拟实验环境中

```bash
{ while :; do echo "Hello Apache Spark"; sleep 0.05; done; } | netcat -l -p 9999
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.1 一个例子：代码片段（3/4）

- 用 spark-submit 提交应用并带上运行参数

```text
root@48feaa001420:~# spark-submit --class NetworkWordCount /data/application/simple-streaming/target/scala-2.13/simple-streaming_2.13-0.1.jar localhost 9999
20/03/26 08:28:39 WARN NativeCodeLoader: Unable to load native-hadoop library for your platform... using builtin-java classes where applicable
```

::: notes
这一步是在把应用真正交给集群执行。讲解时要把主类、资源配置和输入输出参数对应起来，说明任务到底以什么方式跑起来。
:::

## 5.5.1 一个例子：代码片段（4/4）

- 先对照代码理解 使用Docker exec 命令进入到容器中打开另一终端界面

```text
-------------------------------------------
Time: 1585211330000 ms
-------------------------------------------
(Hello,1028)
(Apache,1028)
(Spark,1028)
-------------------------------------------
Time: 1585211340000 ms
-------------------------------------------
(Hello,188)
(Apache,188)
(Spark,188)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.2 迁移对照

- 给出与“词频统计”对应的Structured Streaming写法
- 2）reduceByKeyAndWindow 等窗口聚合 ->
- （4）微批时间间隔 -> trigger(...)（按需设置）

::: notes
为了便于从存量DStream迁移到Spark 4.x主线API，下面给出与“词频统计”对应的Structured Streaming写法。该示例使用Kafka作为输入源，并显式设置watermark与checkpoint目录。

（2）reduceByKeyAndWindow 等窗口聚合 -> groupBy(window(...))。
:::

## 5.5.2 迁移对照

- 先创建 SparkSession 作为统一入口
- 先过滤出真正要参与计算的数据
- 按业务键把相关记录汇到同一组
- 在分组后做聚合计算得到指标结果

```scala
import org.apache.spark.sql.SparkSession
import org.apache.spark.sql.functions._
val spark = SparkSession.builder()
  .appName("StructuredWordCountKafka")
  .getOrCreate()
import spark.implicits._
val lines = spark.readStream
  .format("kafka")
  .option("kafka.bootstrap.servers", "localhost:9092")
  .option("subscribe", "words")
  .option("startingOffsets", "latest")
// ...
```

::: notes
这一页会把 先创建 SparkSession 作为统一入口、先过滤出真正要参与计算的数据 和 按业务键把相关记录汇到同一组 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 5.5.3 StreamingContext（1/2）

- StreamingContext是所有数据流功能切入点
- 通过提供新的SparkContext所需的配置来创建StreamingCon
- 使用现有的SparkContext创建一个StreamingContext
- 可以通过两种方式创建新的StreamingContext

::: notes
StreamingContext是流传输的主要入口点，本质上负责流传输应用程序，包括检查点，转换和对RDD的DStreams的操作。StreamingContext是所有数据流功能切入点，提供了访问方法可以创建来自各种输入源的离散流。

text。

上面StreamingContext两个构造这里的第二个参数是batchDuration，这是数据流被分批的时间间隔。无论使用Spark交互界面或创建一个独立的应用程序，需要创建一个新的StreamingContext。要初始化Spark数据流程序，必须创建一个StreamingContext对象，它是所有Spark数据流功能的主要入口点。
:::

## 5.5.3 StreamingContext（2/2）

- StreamingContext对象可以从SparkConf对象创建
- 代码 5‑4
- appName 是应用在监控界面里显示的名称
- 批次间隔本身则需要结合延迟目标和集群资源来权衡设置

::: notes
（1）如果是在Spark应用程序中，StreamingContext对象可以从SparkConf对象创建。

master 可以是 Standalone、Kubernetes、YARN 的集群地址，也可以是本地模式字符串 local[]。实际部署到集群时，通常不会把 master 硬编码在程序里，而是通过 spark-submit 在提交阶段传入。

本地测试或单元测试时，local[] 则更方便。创建 StreamingContext 时会连带创建底层 SparkContext，可通过 ssc.sparkContext 访问。
:::

## 5.5.3 StreamingContext（1/2）

- 先对照代码理解 （1）如果是在Spark应用程序中

```scala
import org.apache.spark.\_
import org.apache.spark.streaming.\_
val conf = new SparkConf().setAppName(appName).setMaster(master)
val ssc = new StreamingContext(conf, Seconds(1))
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.3 StreamingContext（2/2）

- 先对照代码理解 （2）如果通过spark-shell打开交互界面

```scala
scala> import org.apache.spark.streaming._
import org.apache.spark.streaming._
scala> val ssc = new StreamingContext(sc, Seconds(10))
ssc: org.apache.spark.streaming.StreamingContext =
org.apache.spark.streaming.StreamingContext@3c4231e5
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.4 输入流（1/2）

- 可以使用StreamingContext创建多种类型的输入流
- 这是接收外部数据的抽象类，接收器可以在Spark集群的工作节点上运行
- 第二类是通过额外的接口获得Kafka、Flume、Kinesis等输入流
- 接收器可以确认接收的数据，可以确保在产生故障时不会丢失任何数据

::: notes
例如receiverStream和fileStream。在代码 5‑1中，lines是一个输入离散流，通过socketTextStream从NetCat服务器接收的数据流。每个输入流与接收器（Receiver）对象相关联，该对象接收数据并将其存储在内存中进行处理。

Spark Streaming具有两类输入源，第一类是StreamingContext中直接提供的输入源，例如textFileStream和socketTextStream等，第二类是通过额外的接口获得Kafka、Flume、Kinesis等输入流。如果要在Spark Streaming应用程序中并行接收多个输入源，这将创建多个接收器同时接收多个数据流。

另外，基于接收信息的可靠性也可以用来区分数据接收器。当数据被接收并且复制存储在Spark中时，接收器正确地向可靠的数据源发送确认，可靠的数据源，如Kafka和Flume，实现了传输数据的确认机制，接收器可以确认接收的数据，可以确保在产生故障时不会丢失任何数据。
:::

## 5.5.4 输入流（2/2）

- SocketTextStream已经在代码 5‑1中使用了
- 从hostname：port地址接收数据创建输入流
- hostname：要连接的用于接收数据的主机名
- port：要连接的用于接收数据的端口

::: notes
下面我们介绍几种接收器。SocketTextStream已经在代码 5‑1中使用了，通过TCP套接字接口接收文本数据，创建一个离散流。

port地址接收数据创建输入流，使用TCP套接字接收数据，使用UTF8编码接收文本，换行作为分隔。
:::

## 5.5.4 输入流：代码片段（1/7）

- 通过TCP套接字接口接收文本数据
- 先对照代码理解 除了套接字之外

片段 1
```scala
socketTextStream(
  hostname: String,
  port: Int,
  storageLevel: StorageLevel = StorageLevel.MEMORY_AND_DISK_SER_2
): ReceiverInputDStream[String]
```

片段 2
```scala
def fileStream[K: ClassTag, V: ClassTag, F <: NewInputFormat[K, V]: ClassTag](
  directory: String
): InputDStream[(K, V)]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.4 输入流：代码片段（2/7）

- 先对照代码理解 创建一个输入流

```scala
textFileStream(directory: String): DStream[String]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.4 输入流：代码片段（3/7）

- 用 flatMap 把一条记录展开成多个元素
- 用 map 把输入转换成后续要处理的结构

```scala
scala> import org.apache.spark.streaming._
import org.apache.spark.streaming._
scala> val ssc = new StreamingContext(sc, Seconds(10))
ssc: org.apache.spark.streaming.StreamingContext =
org.apache.spark.streaming.StreamingContext@54a5eff
scala> val lines = ssc.textFileStream("/data/input")
lines: org.apache.spark.streaming.dstream.DStream[String] =
org.apache.spark.streaming.dstream.MappedDStream@3a70acd5
scala> val words = lines.flatMap(_.split(" "))
words: org.apache.spark.streaming.dstream.DStream[String] =
org.apache.spark.streaming.dstream.FlatMappedDStream@c4fc610
// ...
```

::: notes
flatMap 的关键不是做了遍历，而是把一对多的展开放进计算链路。很多词频统计、日志拆词和明细展开都依赖这一步。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 5.5.4 输入流：代码片段（4/7）

- 先对照代码理解 此时

```bash
cp /usr/local/spark/README.md /root/data/input/1.txt
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.4 输入流：代码片段（5/7）

- 先构造一个用于演示的并行数据集

```text
-------------------------------------------
Time: 1585054780000 ms
-------------------------------------------
(stream,1)
(review,1)
(its,1)
([run,1)
(can,6)
(guidance,2)
(have,1)
(locally,2)
// ...
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。
:::

## 5.5.4 输入流：代码片段（6/7）

- 先对照代码理解 要停止流式传输

```scala
def queueStream[T: ClassTag](queue: Queue[RDD[T]], oneAtATime: Boolean = true): InputDStream[T]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 5.5.4 输入流：代码片段（7/7）

- 用 map 把输入转换成后续要处理的结构
- 在分组后做聚合计算得到指标结果

```scala
scala> import org.apache.spark.rdd.RDD
import org.apache.spark.rdd.RDD
scala> import org.apache.spark.streaming.{Seconds, StreamingContext}
import org.apache.spark.streaming.{Seconds, StreamingContext}
scala> import scala.collection.mutable.Queue
import scala.collection.mutable.Queue
scala> val ssc = new StreamingContext(sc, Seconds(1))
ssc: org.apache.spark.streaming.StreamingContext =
org.apache.spark.streaming.StreamingContext@3031d9e9
scala> val rddQueue = new Queue[RDD[Int]]()
rddQueue:
// ...
```

::: notes
map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。

这里才真正把多条记录压成业务指标。讲解时要说明聚合函数在累加什么、合并什么，以及最终留下来的结果代表什么含义。
:::

