<!-- generated by doc-to-ppt-talk-track -->

## 11.6 最佳实践

- 系统配置
- Set everything to be logged to the console
- Set the default spark-shell log level to ERROR. When running the
- Settings to quiet third party logs that are too verbose

::: notes
从工程实践看，Spark 配置通常来自三个入口：应用级的 Spark 属性，适合描述当前作业的运行参数。机器级或进程级的环境变量，适合放在 conf/spark-env.sh 这类文件里。以及日志配置，用来控制输出级别、落盘位置和排障信号。 接下来会依次看 系统配置、Set everything to be logged to the console，以及 Set the default spark-shell log level to ERROR. When running the，后面的实现和案例都会沿着这条线继续展开。
:::

## 11.6 最佳实践：学习路线（1/2）

- 系统配置
- Set everything to be logged to the console
- Set the default spark-shell log level to ERROR. When running the
- Settings to quiet third party logs that are too verbose

::: notes
这一页先看前半段主线，重点包括 系统配置、Set everything to be logged to the console、Set the default spark-shell log level to ERROR. When running the，以及 Settings to quiet third party logs that are too verbose。先把前面的入口和处理顺序放稳，后面再继续往下展开。
:::

## 11.6 最佳实践：学习路线（2/2）

- SPARK-9183: Settings to avoid annoying messages when looking up
- 程序调优

::: notes
这一页补上后半段主线，重点包括 SPARK-9183: Settings to avoid annoying messages when looking up 和 程序调优。把这几步和前面的内容接起来，整节课的推进顺序就完整了。
:::

## 11.6.1 系统配置

- 从工程实践看，Spark 配置通常来自三个入口：应用级的 Spark 属性，适合描述当前作业的运行参数
- 机器级或进程级的环境变量
- 以及日志配置，用来控制输出级别、落盘位置和排障信号

::: notes
适合放在 conf/spark-env.sh 这类文件里。

真正做问题定位时，最重要的不是记住每个入口的名字，而是先分清“这是应用参数、机器参数，还是日志策略”，再去对应位置查找和修改。
:::

## Set everything to be logged

- Set everything to be logged to the

::: notes
这一页重点讨论的是 Set everything to be logged to the console。讲的时候不要只念标题，要把它放回本节主线里，说明它解决什么问题以及为什么需要这一步。
:::

## Set the default spark-shell

- Set the default spark-shell log

::: notes
这一页重点讨论的是 Set the default spark-shell log level to ERROR. When running the。讲的时候不要只念标题，要把它放回本节主线里，说明它解决什么问题以及为什么需要这一步。
:::

## Settings to quiet third

- Settings to quiet third party logs

::: notes
这一页重点讨论的是 Settings to quiet third party logs that are too verbose。讲的时候不要只念标题，要把它放回本节主线里，说明它解决什么问题以及为什么需要这一步。
:::

## SPARK-9183：Settings to（1/3）

- 先对照代码理解 nonexistent UDFs in SparkSQL

```text
代码 11.12
把 `log4j.rootCategory=INFO, console` 改成 `WARN`，通常就能显著减少控制台噪声；如果需要更细粒度排查问题，再临时切到 `INFO` 或 `DEBUG`。真正长期有用的做法，是把应用日志和平台日志都汇聚到统一位置，而不是只盯着交互终端输出。根 Logger 的基本写法如下：
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## SPARK-9183：Settings to（2/3）

- 先对照代码理解 log4j.rootLogger = \[level

```text
代码 11.13
这里最值得掌握的是三件事：日志级别控制“输出多少”，appender 控制“输出到哪里”，layout 控制“长什么样”。在日常排障里，常用的级别主要是 `DEBUG`、`INFO`、`WARN` 和 `ERROR`。如果把根日志级别设成 `INFO`，那么 `DEBUG` 级别的日志默认就不会输出；而线上环境通常更适合从 `WARN` 或 `ERROR` 起步，避免无关噪声淹没有用信息。
常见的输出目标大致有三类：控制台输出，适合本地调试或短任务；滚动文件输出，适合需要保留节点本地日志的场景；集中式日志采集，适合生产环境统一检索。对应到 log4j 术语里，前两类通常由不同的 appender 实现，而生产系统里更关键的其实不是类名本身，而是“能否稳定写到你后续看得到的地方”。
日志格式通常使用 `PatternLayout` 这类可配置布局来完成。一个实用的最小模式一般会包含时间、级别、线程名、logger 名称和消息内容，例如：
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## SPARK-9183：Settings to（3/3）

- 先对照代码理解 %d{yy/MM/dd HH

```text
代码 11.14
这类模式里，`%d` 表示时间，`%p` 表示日志级别，`%t` 表示线程名，`%c` 表示 logger 名称，`%m` 表示消息正文，`%n` 表示换行。知道这些最常用占位符后，通常就足够阅读和调整大部分 Spark 应用的日志配置了；至于更完整的 appender/layout 选项，按具体运行环境查对应版本文档会更稳。
如果某个作业突然变慢，一个实用的排障顺序是：先到 Spark UI 的 Environment 页面确认最终生效的参数，再去 Jobs 页面定位最慢的作业，然后进入对应的 Stages 页面看时间是否主要耗在 Shuffle、数据倾斜或长尾任务上；如果怀疑资源分配不均，就继续看 Executors 页面确认是否有节点明显更忙或失败重试偏多；如果程序本来依赖缓存，还应再看 Storage 页面判断缓存是否真的命中。这样做的好处是先把“配置有没有生效”和“瓶颈发生在哪一层”分开，再决定是否需要调整并行度、缓存策略、序列化方式或代码本身。
如果应用本身是 Structured Streaming，上面这条路径还应再补一个入口：Structured Streaming 页面。它更适合看输入速率、处理速率、批次持续时间、状态算子指标以及 source / sink 的运行状态。很多“批处理页面看起来没问题，但流任务就是越跑越慢”的问题，真正的线索往往藏在这里，例如状态膨胀、迟到数据过多，或者某个 sink 的写出速度已经跟不上输入节奏。
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 11.6.2 程序调优

- 进入调优阶段后，还要先分清程序主要走的是哪条执行路径
- 更可靠的做法是结合 Web UI、运行时统计和最终执行结果

::: notes
这一页先看 进入调优阶段后，还要先分清程序主要走的是哪条执行路径，再看 更可靠的做法是结合 Web UI、运行时统计和最终执行结果。把这两步连起来讲清楚，页面内容和后面的实现流程就能对上。
:::

