<!-- generated by doc-to-ppt-talk-track -->

## 3.2 键值对RDD

- 在许多用例中，需要按某个键进行分组或聚合、联结两个RDD
- 在Scala语言中，Spark键值对RDD的表示是二维元组
- 键值对RDD带有一组API，可以围绕键执行常规操作，例如分组、聚合和联接
- 以下各节将介绍如何创建键值对RDD，以及如何使用关联的转换和操作

::: notes
到目前为止，我们已经使用了RDD，其中每行代表一个值，例如整数或字符串。在许多用例中，需要按某个键进行分组或聚合、联结两个RDD。现在来看一下另一个RDD类型：键值对RDD。 接下来会依次看 键值对RDD、创建，以及 转换，后面的实现和案例都会沿着这条线继续展开。
:::

## 3.2 键值对RDD：学习路线

- 键值对RDD
- 创建
- 转换
- 动作

::: notes
这一页先把 3.2 键值对RDD 的主线串起来，重点包括 键值对RDD、创建、转换，以及 动作。先知道每一步要解决什么问题，后面的细节就更容易跟住。
:::

## 3.2 键值对RDD

- 在许多用例中，需要按某个键进行分组或聚合、联结两个RDD
- 在Scala语言中，Spark键值对RDD的表示是二维元组
- 键值对RDD带有一组API，可以围绕键执行常规操作，例如分组、聚合和联接
- 以下各节将介绍如何创建键值对RDD，以及如何使用关联的转换和操作

::: notes
到目前为止，我们已经使用了RDD，其中每行代表一个值，例如整数或字符串。现在来看一下另一个RDD类型：键值对RDD。键值对的数据格式可以在多种编程语言中找到。

键值对RDD在许多Spark程序中使用。当想要在分布式系统中进行值聚合或重新组合时，需要通过其中的键进行索引，例如有一个包含城市级别人口的数据集，并且想要在州级别汇总，那么需要按州对这些行进行分组，并对每个州所有城市的人口求和。

另一个例子是提取客户标识作为键，以查看所有客户的订单。要想满足键值对RDD的要求，每一行必须包含一个元组，其中第一个元素代表键，第二个元素代表值。键和值的类型可以是简单的类型，例如整数或字符串，也可以是复杂的类型，例如对象或值的集合或另一个元组。

代码 3.1 上面的代码创建了键值对RDD，每一行为一个元组，其中键是长度，值是单词。它们被包裹在一对括号内。一旦以这种方式排列了每一行，我们就可以通过按键分组轻松发现长度相同的单词。
:::

## 3.2 键值对RDD：代码片段

- 先构造一个用于演示的并行数据集
- 用 map 把输入转换成后续要处理的结构
- 通过 action 触发前面的惰性计算真正执行

```scala
scala> val rdd = sc.parallelize(List("Spark","is","an", "amazing",
"piece", "of","technology"))
rdd: org.apache.spark.rdd.RDD[String] = ParallelCollectionRDD[0] at
parallelize at \<console\>:24
scala> val pairRDD = rdd.map(w => (w.length,w))
pairRDD: org.apache.spark.rdd.RDD[(Int, String)] =
MapPartitionsRDD[1] at map at \<console\>:25
scala> pairRDD.collect().foreach(println)
(5,Spark)
(2,is)
(2,an)
// ...
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。

前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.1 创建（1/2）

- 创建键值对最常的方法
- 或加载特定数据时也可以获得键值对，有很多数据格式将被创建为键值对
- 另一种方法是通过内存中的集合创建键值对
- 虽然大多数Spark操作适用于包含任何类型对象的RDD

::: notes
将看看怎样创建键值对。创建键值对最常的方法包括：使用已经存在的非键值对。

但是几个特殊操作只能在键值对的RDD上使用，例如按键分组或聚合元素，这些操作都需要进行分布式洗牌。在Scala语言中，这些操作可以在包含Tuple2对象的RDD上自动可用。键值对操作在PairRDDFunctions类中，自动封装在元组RDD上。
:::

## 3.2.1 创建（2/2）

- 语法解释
- Scala元组结合件多个固定数量的项目在一起，使它们可以被作为一个整体传递
- 代码 3.2 这是语法方糖，是下面代码的简写方式
- 代码 3.3 一个元组的实际类型取决于它包含的元素和这些元素的类型和数目

::: notes
不像一个数组或列表，元组可以容纳不同类型的对象，但它们也是不可改变的。这里是一个元组持有整数、字符串和Console，如下的一个例子。

所以，该类型 (99, "Luftballons") 是 Tuple2Int, String。
:::

## 3.2.1 创建：代码片段（1/7）

- 先对照代码理解 Scala元组结合件多个固定数量的项目在一起
- 先对照代码理解 代码 3.2 这是语法方糖，是下面代码的简写方式： 的执行步骤

片段 1
```text
val t = (1, "hello", Console)
```

片段 2
```text
val t = new Tuple3(1, "hello", Console)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.2.1 创建：代码片段（2/7）

- Tuple2
- 先对照代码理解 代码 3.4 要访问的元组t的元素

片段 1
```text
val t = (4,3,2,1)
```

片段 2
```text
val sum = t.\_1 + t.\_2 + t.\_3 + t.\_4
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.2.1 创建：代码片段（3/7）

- 先构造一个用于演示的并行数据集

```scala
scala> val data = sc.parallelize(List(("key1", 1), ("Key2", 2),
("Key3", 2)))
data: org.apache.spark.rdd.RDD[(String, Int)] =
ParallelCollectionRDD[16] at parallelize at \<console\>:24
scala> data.saveAsSequenceFile("/data/seq-output")
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。
:::

## 3.2.1 创建：代码片段（4/7）

- 通过 action 触发前面的惰性计算真正执行

```scala
scala> import org.apache.hadoop.io.{Text, IntWritable}
import org.apache.hadoop.io.{Text, IntWritable}
scala> val result = sc.sequenceFile("/data/seq-output",
classOf[Text], classOf[IntWritable]).map{case (x, y) =>
(x.toString, y.get())}
result: org.apache.spark.rdd.RDD[(String, Int)] =
MapPartitionsRDD[19] at map at \<console\>:26
scala> result.collect
res11: Array[(String, Int)] = Array((key1,1), (Kay2,2), (Key3,2))
```

::: notes
前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.1 创建：代码片段（5/7）

- 先对照代码理解 Scala 提供了强大的模式匹配机制
- 先对照代码理解 代码 3.8 定义langs序列（Seq）变量

片段 1
```scala
val langs = Seq(
  ("Scala", "Martin", "Odersky"),
  ("Clojure", "Rich", "Hickey"),
  ("Lisp", "John", "McCarthy")
)
```

片段 2
```scala
for (tuple <- langs) {
  tuple match {
    case ("Scala", _, _) =>
      println("Found Scala")
    case (lang, first, last) =>
// ...
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.2.1 创建：代码片段（6/7）

- 先对照代码理解 代码 3.9 在for循环中

```text
Found Scala
Found other language: Clojure (Rich, Hickey)
Found other language: Lisp (John, McCarthy)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.2.1 创建：代码片段（7/7）

- 先构造一个用于演示的并行数据集
- 通过 action 触发前面的惰性计算真正执行

```scala
scala> val dist1 = Array(("INGLESIDE",1), ("SOUTHERN",1), ("PARK",1),
("NORTHERN",1))
dist1: Array[(String, Int)] = Array((INGLESIDE,1), (SOUTHERN,1),
(PARK,1), (NORTHERN,1))
scala> val dist1RDD = sc.parallelize(dist1)
dist1RDD: org.apache.spark.rdd.RDD[(String, Int)] =
ParallelCollectionRDD[44] at parallelize at \<console\>:30
scala> dist1RDD.collect
res29: Array[(String, Int)] = Array((INGLESIDE,1), (SOUTHERN,1),
(PARK,1), (NORTHERN,1))
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.2 转换（1/2）

- 的部分总结了键值对常用转换，然后分别详细介绍几个转换
- 基于一个键值对RDD的转换
- 创建一个键值对RDD
- 代码 3.12

::: notes
键值对RDD允许使用标准RDD可用的所有转换，由于键值对包含元组，需要在转换方法中传递可以在元组上操作的函数。下面的部分总结了键值对常用转换，然后分别详细介绍几个转换。
:::

## 3.2.2 转换（2/2）

- 调用包含(K, V)的数据集，返回的结果也为(K, V)
- 代码 3.13
- 调用包含(K, V)的数据集，返回(K, Iterable\<V\>)
- 代码 3.14

::: notes
数据集中的每个键对应的所有值被聚集，使用给定的汇总功能func，其类型必须为(V, V) => V。像groupByKey，汇总任务的数量是通过第二个可选的参数numPartitions配置，这个参数设置RDD的分区数。

如果分组的目的是为了对每个键执行聚集，如总和或平均值，使用reduceByKey或aggregateByKey将产生更好的性能。默认情况下，输出的并行任务数取决于RDD谱系中父RDD的分区数，可以通过一个可选的参数numPartitions来设置不同数量的任务。
:::

## 3.2.2 转换：代码片段（1/8）

- 先构造一个用于演示的并行数据集
- 在分组后做聚合计算得到指标结果

片段 1
```scala
scala> val rdd = sc.parallelize(List((1, 2), (3, 4), (3, 6)))
rdd: org.apache.spark.rdd.RDD[(Int, Int)] =
ParallelCollectionRDD[15] at parallelize at \<console\>:24
```

片段 2
```scala
scala> rdd.reduceByKey((x, y) => x + y).collect
res5: Array[(Int, Int)] = Array((1,2), (3,10))
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

这里才真正把多条记录压成业务指标。讲解时要说明聚合函数在累加什么、合并什么，以及最终留下来的结果代表什么含义。
:::

## 3.2.2 转换：代码片段（2/8）

- 按业务键把相关记录汇到同一组
- 通过 action 触发前面的惰性计算真正执行

片段 1
```scala
scala> rdd.groupByKey().collect
res6: Array[(Int, Iterable[Int])] = Array((1,CompactBuffer(2)),
(3,CompactBuffer(4, 6)))
```

片段 2
```scala
scala> rdd.mapValues(x => x+1).collect
res11: Array[(Int, Int)] = Array((1,3), (3,5), (3,7))
```

::: notes
分组这一步决定了结果的统计粒度。只要把按什么键聚说清楚，听众就能知道最后的指标到底是按用户、订单还是时间窗口汇总出来的。

前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.2 转换：代码片段（3/8）

- 通过 action 触发前面的惰性计算真正执行

片段 1
```scala
scala> rdd.flatMapValues(x => (x to 5)).collect
res13: Array[(Int, Int)] = Array((1,2), (1,3), (1,4), (1,5), (3,4),
(3,5))
```

片段 2
```scala
scala> rdd.keys.collect
res15: Array[Int] = Array(1, 3, 3)
```

::: notes
前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.2 转换：代码片段（4/8）

- 通过 action 触发前面的惰性计算真正执行

片段 1
```scala
scala> rdd.values.collect
res20: Array[Int] = Array(2, 4, 6)
```

片段 2
```scala
scala> rdd.sortByKey().collect
res25: Array[(Int, Int)] = Array((1,2), (3,4), (3,6))
```

::: notes
前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.2 转换：代码片段（5/8）

- 先构造一个用于演示的并行数据集
- 用 map 把输入转换成后续要处理的结构
- 通过 action 触发前面的惰性计算真正执行

```scala
scala> val pairRDD = sc.parallelize(List( ("cat",2), ("cat", 5),
("mouse", 4),("cat", 12), ("dog", 12), ("mouse", 2)), 2)
pairRDD: org.apache.spark.rdd.RDD[(String, Int)] =
ParallelCollectionRDD[1] at parallelize at \<console\>:24
scala> def myfunc(index: Int, iter: Iterator[(String, Int)]) :
Iterator[String] = {
| iter.map(x => "[partID:" + index + ", val: " + x + "]")
| }
myfunc: (index: Int, iter: Iterator[(String, Int)])Iterator[String]
scala> pairRDD.mapPartitionsWithIndex(myfunc).collect
res0: Array[String] = Array([partID:0, val: (cat,2)], [partID:0,
// ...
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。

前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.2 转换：代码片段（6/8）

- 先构造一个用于演示的并行数据集
- 通过 action 触发前面的惰性计算真正执行

片段 1
```scala
scala> val rdd = sc.parallelize(List((1, 2), (3, 4), (3, 6)))
rdd: org.apache.spark.rdd.RDD[(Int, Int)] =
ParallelCollectionRDD[42] at parallelize at \<console\>:24
scala> val other = sc.parallelize(List((3,9)))
other: org.apache.spark.rdd.RDD[(Int, Int)] =
ParallelCollectionRDD[43] at parallelize at \<console\>:24
```

片段 2
```scala
scala> rdd.subtractByKey(other).collect
res27: Array[(Int, Int)] = Array((1,2))
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.2 转换：代码片段（7/8）

- 把不同来源的数据按关联键接起来
- 通过 action 触发前面的惰性计算真正执行

片段 1
```scala
scala> rdd.join(other).collect
res28: Array[(Int, (Int, Int))] = Array((3,(4,9)), (3,(6,9)))
```

片段 2
```scala
scala> rdd.rightOuterJoin(other).collect
res30: Array[(Int, (Option[Int], Int))] = Array((3,(Some(4),9)),
(3,(Some(6),9)))
```

::: notes
连接操作的关键在于主键、外键和连接条件。讲的时候要说清楚两边各提供什么信息，以及接起来以后想补足哪部分业务语义。

前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.2 转换：代码片段（8/8）

- 通过 action 触发前面的惰性计算真正执行

片段 1
```scala
scala> rdd.leftOuterJoin(other).collect
res31: Array[(Int, (Int, Option[Int]))] = Array((1,(2,None)),
(3,(4,Some(9))), (3,(6,Some(9))))
```

片段 2
```scala
scala> rdd.cogroup(other).collect
res32: Array[(Int, (Iterable[Int], Iterable[Int]))] =
Array((1,(CompactBuffer(2),CompactBuffer())), (3,(CompactBuffer(4,
6),CompactBuffer(9))))
```

::: notes
前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

## 3.2.3 动作（1/2）

- 所有在基础RDD上提供的传统转换操作也可用在键值对RDD上
- 代码 3.37
- 要处理非常大的结果，可以考虑使用
- 代码 3.38 其将返回RDD T，Long而不是Map

::: notes
与转换一样，所有在基础RDD上提供的传统转换操作也可用在键值对RDD上，当然键值对RDD可以使用一些额外的操作，首先创建一个RDD。

对每个键进行计数，只有当返回的结果Map预计很小时，才应该使用此方法，因为整个内容都会加载到驱动程序的内存中。
:::

## 3.2.3 动作（2/2）

- 代码 3.39
- 对关键值RDD起作用并将其转换为Scala Map以保留其键值结构
- 代码 3.40
- 返回与提供键相关联的所有值

::: notes
与collect()类似，但对关键值RDD起作用并将其转换为Scala Map以保留其键值结构，如果键值对RDD中同一个键有多个值，则每个键中只有一个值会保留在返回的Map中。因为所有的数据都加载到驱动程序的内存中，所以只有在结果数据很小时才应使用此方法。

如果RDD具有已知的分区程序，则只需搜索该键映射到的分区即可高效地执行此操作。
:::

## 3.2.3 动作：代码片段（1/5）

- 先构造一个用于演示的并行数据集

```scala
scala> val rdd = sc.parallelize(List((1, 2), (3, 4), (3, 6)))
rdd: org.apache.spark.rdd.RDD[(Int, Int)] =
ParallelCollectionRDD[15] at parallelize at \<console\>:24
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。
:::

## 3.2.3 动作：代码片段（2/5）

- 先对照代码理解 对每个键进行计数

```text
rdd.mapValues（\_ =\> 1L）.reduceByKey（\_ + \_）
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.2.3 动作：代码片段（3/5）

- 先对照代码理解 代码 3.38 其将返回RDD \[T

```scala
scala> rdd.countByKey()
res74: scala.collection.Map[Int,Long] = Map(1 -> 1, 3 -> 2)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.2.3 动作：代码片段（4/5）

- 先对照代码理解 与collect()类似

```scala
scala> rdd.collectAsMap()
res80: scala.collection.Map[Int,Int] = Map(1 -> 2, 3 -> 6)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.2.3 动作：代码片段（5/5）

- 先对照代码理解 返回与提供键相关联的所有值。如果RDD具有已知的分区程序

```scala
scala> rdd.lookup(3)
res91: Seq[Int] = WrappedArray(4, 6)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

