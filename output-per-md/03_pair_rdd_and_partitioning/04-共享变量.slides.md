<!-- generated by doc-to-ppt-talk-track -->

## 3.4 共享变量

- 共享变量是许多函数和方法必须并行使用的变量，可以在并行操作中使用

::: notes
共享变量是许多函数和方法必须并行使用的变量，可以在并行操作中使用。通常，当Spark操作，例如map()或reduce()，在远程集群节点上执行时，函数被传递到操作中，而且函数所需要的变量会在每个节点任务上拷贝副本，所以Spark操作可以独立的执行。这些变量被复制，并且在远程计算机上的变量更新都不会传播回驱动程序。 接下来会依次看 共享变量、广播变量，以及 累加器，后面的实现和案例都会沿着这条线继续展开。
:::

## 3.4.1 广播变量（1/2）

- 广播变量是所有执行程序之间的共享变量
- 这使扩展性更强的方法可以将广播变量分发给所有执行程序
- 广播变量是通过调用SparkContext.broadcast(v)方法
- 广播变量也可以不仅仅在原始数据类型上创建

::: notes
是在驱动程序中创建的，然后在执行程序上是只读的。广播变量使Spark的操作可以在每台计算机上保留一个只读变量，而不用将其副本与任务一起发送，可以使用它们以有效的方式为每个节点提供大型输入数据集的副本。

可以在Spark集群中广播整个数据集，以便执行器可以访问广播的数据。执行器中运行的所有任务都可以访问广播变量。广播使用各种优化的方法使广播数据可供所有执行器访问。

Spark的动作是通过一组阶段执行的，这些阶段被分布式洗牌操作分割。Spark自动广播每个阶段中任务所需的通用数据。以这种方式广播的数据以序列化形式缓存，并在运行每个任务之前反序列化。

如下面的示例所示，我们将从驱动程序广播HashMap。
:::

## 3.4.1 广播变量（2/2）

- 有一种方法可以从所有执行程序的内存中删除广播变量
- 我们还可以销毁广播变量，将其从所有执行器中完全删除，并且驱动程序也无法访问它们

::: notes
广播变量确实会占用所有执行器的内存，而且取决于广播变量中包含的数据大小，这有时可能会导致资源问题。在广播变量上调用unpersist()会从所有执行器的内存缓存中删除广播变量的数据，以释放资源。如果再次使用该变量，则数据将重新传输给执行器，以便再次使用。

这对于在整个集群中最佳地管理资源非常有帮助。在广播变量上调用destroy()会破坏与指定广播变量相关的所有数据和元数据。广播变量一旦销毁将无法再次使用，而必须重新创建，以下是销毁广播变量的示例。
:::

## 3.4.1 广播变量：代码片段（1/4）

- 先构造一个用于演示的并行数据集
- 用 map 把输入转换成后续要处理的结构

```scala
scala> val rdd_one = sc.parallelize(Seq(1,2,3))
rdd_one: org.apache.spark.rdd.RDD[Int] = ParallelCollectionRDD[0]
at parallelize at \<console\>:24
scala> val i = 5
i: Int = 5
scala> val bi = sc.broadcast(i)
bi: org.apache.spark.broadcast.Broadcast[Int] = Broadcast(0)
scala> bi.value
res0: Int = 5
scala> rdd_one.take(5)
res1: Array[Int] = Array(1, 2, 3)
// ...
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 3.4.1 广播变量：代码片段（2/4）

- 先构造一个用于演示的并行数据集
- 用 map 把输入转换成后续要处理的结构

```scala
scala> val rdd_one = sc.parallelize(Seq(1,2,3))
rdd_one: org.apache.spark.rdd.RDD[Int] = ParallelCollectionRDD[2]
at parallelize at \<console\>:24
scala> val m = scala.collection.mutable.HashMap(1 -> 2, 2 -> 3, 3 ->
4)
m: scala.collection.mutable.HashMap[Int,Int] = Map(2 -> 3, 1 -> 2, 3
-> 4)
scala> val bm = sc.broadcast(m)
bm:
org.apache.spark.broadcast.Broadcast[scala.collection.mutable.HashMap[Int,Int]]
= Broadcast(7)
// ...
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 3.4.1 广播变量：代码片段（3/4）

- 先构造一个用于演示的并行数据集
- 用 map 把输入转换成后续要处理的结构

```scala
scala> val rdd_one = sc.parallelize(Seq(1,2,3))
rdd_one: org.apache.spark.rdd.RDD[Int] = ParallelCollectionRDD[4]
at parallelize at \<console\>:24
scala> val k = 5
k: Int = 5
scala> val bk = sc.broadcast(k)
bk: org.apache.spark.broadcast.Broadcast[Int] = Broadcast(11)
scala> rdd_one.map(j => j + bk.value).take(5)
res4: Array[Int] = Array(6, 7, 8)
scala> bk.unpersist
scala> rdd_one.map(j => j + bk.value).take(5)
res6: Array[Int] = Array(6, 7, 8)
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 3.4.1 广播变量：代码片段（4/4）

- 先构造一个用于演示的并行数据集
- 用 map 把输入转换成后续要处理的结构

```scala
scala> val rdd_one = sc.parallelize(Seq(1,2,3))
rdd_one: org.apache.spark.rdd.RDD[Int] = ParallelCollectionRDD[7]
at parallelize at \<console\>:24
scala> val k = 5
k: Int = 5
scala> val bk = sc.broadcast(k)
bk: org.apache.spark.broadcast.Broadcast[Int] = Broadcast(18)
scala> rdd_one.map(j => j + bk.value).take(5)
res7: Array[Int] = Array(6, 7, 8)
scala> bk.destroy
scala> rdd_one.map(j => j + bk.value).take(5)
// ...
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 3.4.2 累加器（1/2）

- 累加器是执行器之间的共享变量，通常用于向Spark程序添加计数器
- 内置的累加器可用于许多用例，其中包括
- （1）LongAccumulator：用于计算64位整数的和、计数和平均值
- （2）DoubleAccumulator：用于计算双精度浮点数的总和、计数和平均值

::: notes
累加器是仅通过关联和交换操作进行累加的变量，因此可以有效地被并行操作支持，可用于实现计数器（如MapReduce中的计数器）或求和。Spark本机支持数字类型的累加器，也可以添加对新类型的支持。我们可以创建命名或未命名的累加器。
:::

## 3.4.2 累加器（2/2）

- （3）CollectionAccumulator T：用于收集元素列表
- 尽管上面的例子使用了内置支持的整数类型累加器
- add()用于将另一个值添加到累加器
- merge()将另一个相同类型的累加器合并到该方法中

::: notes
我们也可以通过将AccumulatorV2子类化来创建自己的类型。AccumulatorV2抽象类具有几种必须重写的方法：reset()用于将累加器重置为零。

API文档中包含其他必须重写的方法。接下来，我们将看一个自定义累加器的实际示例。同样，我们将为此使用statesPopulation CSV文件。
:::

## 3.4.2 累加器：代码片段（1/6）

- 把原始数据从存储读入 Spark
- 用 map 把输入转换成后续要处理的结构

```scala
scala> val statesPopulationRDD =
sc.textFile("/data/statesPopulation.csv")
statesPopulationRDD: org.apache.spark.rdd.RDD[String] =
/data/statesPopulation.csv MapPartitionsRDD[1] at textFile at
\<console\>:24
scala> val acc1 = sc.longAccumulator("acc1")
acc1: org.apache.spark.util.LongAccumulator = LongAccumulator(id: 0,
name: Some(acc1), value: 0)
scala> val someRDD = statesPopulationRDD.map(x => {acc1.add(1); x})
someRDD: org.apache.spark.rdd.RDD[String] = MapPartitionsRDD[2] at
map at \<console\>:27
// ...
```

::: notes
这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 3.4.2 累加器：代码片段（2/6）

- 先对照代码理解 步骤1.导入包含AccumulatorV2类的软件包
- 先对照代码理解 步骤2.包含年份和人口的案例类别： 的执行步骤

片段 1
```scala
import org.apache.spark.util.AccumulatorV2
```

片段 2
```scala
case class YearPopulation(year: Int, population: Long)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.4.2 累加器：代码片段（3/6）

- 先对照代码理解 步骤3.

```scala
class StateAccumulator extends AccumulatorV2[YearPopulation, YearPopulation] {
  // 为年份和人口数量声明两个变量
  private var year = 0
  private var population: Long = 0L
  // 如果年份和人口数量为零返回 isZero
  override def isZero: Boolean = year == 0 && population == 0L
  // 拷贝累加器并且返回一个新的累加器
  override def copy(): StateAccumulator = {
    val newAcc = new StateAccumulator
    newAcc.year = this.year
    newAcc.population = this.population
// ...
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.4.2 累加器：代码片段（4/6）

- 先对照代码理解
- 把原始数据从存储读入 Spark

片段 1
```scala
val statePopAcc = new StateAccumulator
sc.register(statePopAcc, "statePopAcc")
```

片段 2
```scala
val statesPopulationRDD =
  sc.textFile("statesPopulation.csv").filter(_.split(",")(0) != "State")
statesPopulationRDD.take(10)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。
:::

## 3.4.2 累加器：代码片段（5/6）

- 用 map 把输入转换成后续要处理的结构
- 先对照代码理解 步骤7.现在我们可以检查StateAccumulator的值

片段 1
```scala
statesPopulationRDD.map(x => {
  val toks = x.split(",")
  val year = toks(1).toInt
  val pop = toks(2).toLong
  statePopAcc.add(YearPopulation(year, pop))
// ...
```

片段 2
```scala
statePopAcc.value
```

::: notes
map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 3.4.2 累加器：代码片段（6/6）

- 先对照代码理解 上面的步骤是自定义累加器的分步骤讲解

```scala
scala> :load /data/code/AccumulatorsExample.txt
Loading /data/code/AccumulatorsExample.txt...
import org.apache.spark.util.AccumulatorV2
defined class YearPopulation
defined class StateAccumulator
statePopAcc: StateAccumulator = Un-registered Accumulator:
StateAccumulator
statesPopulationRDD: org.apache.spark.rdd.RDD[String] =
MapPartitionsRDD[2] at filter at /data/code/AccumulatorsExample.txt:25
res1: Array[String] = Array(Alabama,2010,4785492, Alaska,2010,714031,
Arizona,2010,6408312, Arkansas,2010,2921995, California,2010,37332685,
// ...
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

