<!-- generated by doc-to-ppt-talk-track -->

## 6.5 创建和探索图

- 实际上，图可以用于表示任何复杂的系统，因为它描述了系统组件之间的交互
- 我们将遇到的第一种通信网络是电邮通信图
- 开发GraphX程序
- 代码 6‑12

::: notes
在上一节中，我们构建了一个微型的社交网络，从本节开始我们将使用来自各种应用程序的真实数据集。实际上，图可以用于表示任何复杂的系统，因为它描述了系统组件之间的交互。尽管不同系统的形式、大小、性质和粒度各不相同，但是图论提供了一种通用语言和一组工具来表示和分析复杂系统。 接下来会依次看 创建和探索图、属性图，以及 构建器，后面的实现和案例都会沿着这条线继续展开。
:::

## 6.5 创建和探索图：学习路线（1/2）

- 创建和探索图
- 属性图
- 构建器
- 创建图

::: notes
这一页先看前半段主线，重点包括 创建和探索图、属性图、构建器，以及 创建图。先把前面的入口和处理顺序放稳，后面再继续往下展开。
:::

## 6.5 创建和探索图：学习路线（2/2）

- 探索图

::: notes
这一页补上后半段主线，重点包括 探索图。把这几步和前面的内容接起来，整节课的推进顺序就完整了。
:::

## 6.5 创建和探索图（1/2）

- 实际上，图可以用于表示任何复杂的系统，因为它描述了系统组件之间的交互
- 我们将遇到的第一种通信网络是电邮通信图
- 开发GraphX程序
- 代码 6‑12

::: notes
在上一节中，我们构建了一个微型的社交网络，从本节开始我们将使用来自各种应用程序的真实数据集。尽管不同系统的形式、大小、性质和粒度各不相同，但是图论提供了一种通用语言和一组工具来表示和分析复杂系统。

在这一节，我们将遇到的第一种通信网络是电邮通信图，可以将组织内交换电子邮件的历史记录映射到通信图，以了解组织背后某种隐藏的结构。这样的图表还可以用于确定组织中有影响力的人员或枢纽，但是不一定是高级职位。电邮通信图是有向图的典型示例，因为每封电子邮件都是从起始节点链接到目标节点。

作为Apache Spark组件，GraphX 主要实现了图的并行计算，位于Spark核心组件之上的分布式图处理框架。开发GraphX程序，首先需要将Spark和GraphX函数库导入到项目中，如下所示。
:::

## 6.5 创建和探索图（2/2）

- 如果不使用Spark交互界面，还需要一个SparkContext

::: notes
这一页重点放在 如果不使用Spark交互界面，还需要一个SparkContext。只要把这一步的输入、输出和作用说清楚，后面的内容就容易接上。
:::

## 6.5 创建和探索图：代码片段

- 先对照代码理解 作为Apache Spark组件

```scala
import org.apache.spark.\_
import org.apache.spark.graphx.\_
// To make some of the examples work we will also need RDD
import org.apache.spark.rdd.RDD
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.1 属性图（1/2）

- 属性图是一个有向多重图，用户定义的对象附加到每个点和边
- 当对象类型是原始数据类型时，int、double等时
- 代码 6‑13
- 像 RDD 一样，属性图也是不可变、分布式且具备容错能力的数据结构

::: notes
有向多重图是具有潜在多个平行边的有向图，共享相同起始点和目标点的，支持平行边的能力简化了在同一点之间可以存在多个关系的建模场景（如同事和朋友）。每个点都有唯一的 64位长点标识符VertexId，GraphX不会对点标识符施加任何排序约束，类似地边具有对应的起始点和目标点标识符。

属性图在点类型VD和边类型ED上进行参数化，这些对象类型分别与每个点和边相关联。当对象类型是原始数据类型时，例如int、double等时，GraphX优化点和边类型的表示，通过将其存储在专门的数组中来减少内存占用。在某些情况下，可能希望在同一个图中具有不同属性类型的点，可以通过继承来实现，例如将用户和产品建模为二分图，可能会执行以下操作。

对图的值或结构做修改时，GraphX 会返回一个新图。
:::

## 6.5.1 属性图（2/2）

- 图结构会通过顶点切分等启发式方法分布到不同执行器上，故障时也可以基于谱系重新恢复
- 代码 6‑14
- VertexRDDVD和EdgeRDDED提供了基于图计算和利用内部优化的附
- 图例 6‑7 属性图的定义

::: notes
未受影响的结构、属性和索引通常会被复用，因此不必把整张图都重新复制一遍。逻辑上，一个属性图可以看成两组带类型的集合：vertices 和 edges，其中顶点和边都带有各自的属性，因此 Graph 类会暴露访问两者的成员。

类VertexRDDVD和EdgeRDDED扩展和优化了RDD(VertexId, VD)和RDDEdge [ED\] 版本，VertexRDDVD和EdgeRDDED提供了基于图计算和利用内部优化的附加功能。假设要构建一个由GraphX项目中的各种协作者组成的属性图，点属性可能包含用户名和职业，可以用描述协作者之间关系的字符串来注释边。
:::

## 6.5.1 属性图：代码片段（1/6）

- 先对照代码理解 属性图在点类型VD和边类型ED上进行参数化
- 先对照代码理解 像 RDD 一样

片段 1
```scala
class VertexProperty()
case class UserProperty(name: String) extends VertexProperty
case class ProductProperty(name: String, price: Double) extends VertexProperty
// The graph might then have the type:
var graph: Graph[VertexProperty, String] = null
```

片段 2
```scala
class Graph[VD, ED] {
  val vertices: VertexRDD[VD]
  val edges: EdgeRDD[ED]
}
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.1 属性图：代码片段（2/6）

- 先对照代码理解 图例 6‑7 属性图的定义 的执行步骤

```scala
val userGraph: Graph[(String, String), String]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.1 属性图：代码片段（3/6）

- 先构造一个用于演示的并行数据集

```scala
val sc: SparkContext
// 为点创建RDD
val users: RDD[(VertexId, (String, String))] =
  sc.parallelize(
    Array(
      (3L, ("rxin", "student")),
      (7L, ("jgonzal", "postdoc")),
      (5L, ("franklin", "prof")),
      (2L, ("istoica", "prof"))
    )
  )
// ...
```

::: notes
这里没有从外部读数据，而是直接在程序里构造样例集合。它的目的通常是先把算子链路讲清楚，再替换成真实数据源。
:::

## 6.5.1 属性图：代码片段（4/6）

- 先过滤出真正要参与计算的数据
- 因此使用case表达式来解构元组

片段 1
```scala
val graph: Graph[(String, String), String] // Constructed from above
// 计算所有 postdoc 用户
graph.vertices.filter { case (id, (name, pos)) => pos == "postdoc" }.count
// 计算所有边，满足 srcId > dstId
graph.edges.filter(e => e.srcId > e.dstId).count
```

片段 2
```scala
graph.edges.filter { case Edge(src, dst, prop) => src > dst }.count
```

::: notes
这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.1 属性图：代码片段（5/6）

- 先用 SQL 描述需要的查询逻辑

```sql
SELECT src.id, dst.id, src.attr, e.attr, dst.attr
FROM edges AS e LEFT JOIN vertices AS src, vertices AS dst
ON e.srcId = src.Id AND e.dstId = dst.Id
```

::: notes
这里把业务需求翻译成查询语义。讲解时重点不是逐字念 SQL，而是说明筛选、连接和聚合分别对应什么业务问题。
:::

## 6.5.1 属性图：代码片段（6/6）

- 用 map 把输入转换成后续要处理的结构

```scala
val graph: Graph[(String, String), String] // Constructed from above
// 使用三元组视图创建RDD
val facts: RDD[String] =
  graph.triplets.map(triplet =>
    triplet.srcAttr._1 + " is the " + triplet.attr + " of " + triplet.dstAttr._1
  )
facts.collect.foreach(println(_))
```

::: notes
map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 6.5.1 属性图：图示（1）

![性图](../../md/media/06_graph_processing/media/image7.jpeg){width=72%}

::: notes
类VertexRDDVD和EdgeRDDED扩展和优化了RDD(VertexId, VD)和RDDEdge [ED\] 版本，VertexRDDVD和EdgeRDDED提供了基于图计算和利用内部优化的附加功能。假设要构建一个由GraphX项目中的各种协作者组成的属性图，点属性可能包含用户名和职业，可以用描述协作者之间关系的字符串来注释边。
:::

## 6.5.1 属性图：图示（2）

![缘三联](../../md/media/06_graph_processing/media/image8.jpeg){width=72%}

::: notes
这一页围绕 属性图：图示（2） 展开，讲解时按原文把定义、条件和结论顺着说明白。
:::

## 6.5.2 构建器（1/2）

- 第一个是代码 6‑7中已经看到的Graph工厂方法
- 代码 6‑21
- defaultVertexAttr属性用于为点分配默认属性
- 代码 6‑22

::: notes
在GraphX中，有四个用于构建属性图的函数，每一个都要求应该以指定的方式构造数据。第一个是代码 6‑7中已经看到的Graph工厂方法，在名为Graph的伴随对象的apply()方法中定义，如下所示。

如我们之前所见，此函数采用两个RDD集合：RDD (VertexId, VD)和RDD Edge [ED\]作为点和边的参数，并且构造了Graph VD，ED参数。defaultVertexAttr属性用于为点分配默认属性，该点存在于边edges中但不存在于顶点vertices中的，如果容易获得边和点RDD集合时，使用Graph工厂方法很方便。
:::

## 6.5.2 构建器（2/2）

- 该函数以包含边列表的文件路径作为参数，其中每一行代表图的边，由两个整数的表示为：sourceID和destinationID
- 第三个函数Graph.fromEdges()使我们可以从RDD Edge
- 代码 6‑23
- 一个图构建器函数是Graph.fromEdgeTuples

::: notes
在阅读文件列表时，该函数将忽略以“＃”开头的任何注释行，然后根据指定的边以及相应的点构造一个图。minEdgePartitions参数指定要生成的最小边分区数，如果HDFS文件具有更多块则边分区可能比指定的更多。

与GraphLoader.edgeListFile()相似，第三个函数Graph.fromEdges()使我们可以从RDD Edge [ED\]集合创建图，使用edges指定的VertexID参数以及defaultValue参数作为默认属性自动创建点。

最后一个图构建器函数是Graph.fromEdgeTuples，它仅从边元组的RDD创建图，形式为RDD (VertexId, VertexId)类型的集合。默认情况下，为边缘分配属性值1。
:::

## 6.5.2 构建器：代码片段（1/4）

- 先对照代码理解 在GraphX中

```scala
def apply[VD, ED](
  vertices: RDD[(VertexId, VD)],
  edges: RDD[Edge[ED]],
  defaultVertexAttr: VD = null
): Graph[VD, ED]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.2 构建器：代码片段（2/4）

- 先对照代码理解 如我们之前所见

```scala
def edgeListFile(
  sc: SparkContext,
  path: String,
  canonicalOrientation: Boolean = false,
  minEdgePartitions: Int = 1
): Graph[Int, Int]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.2 构建器：代码片段（3/4）

- 先对照代码理解 与GraphLoader.edgeListFile()相似

```scala
def fromEdges[VD, ED](
  edges: RDD[Edge[ED]],
  defaultValue: VD
): Graph[VD, ED]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.2 构建器：代码片段（4/4）

- 先对照代码理解 最后一个图构建器函数是Graph.fromEdgeTuples

```scala
def fromEdgeTuples[VD](
  rawEdges: RDD[(VertexId, VertexId)],
  defaultValue: VD,
  uniqueEdges: Option[PartitionStrategy] = None
): Graph[VD, Int]
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.3 创建图（1/2）

- 如果之前重启过 Spark Shell，需要先重新导入 GraphX 相关包
- 可以直接把这个路径传给 GraphLoader.edgeListFile 来建图
- 代码 6‑25
- 代码 6‑26

::: notes
开始在 Spark 交互环境中构建三种不同类型的图：电邮通信网络有向图、成分与化合物连接形成的二分图，以及由构造器生成的多图。第一个例子是电邮通信网络。实验环境里提供了 /data/emailEnron.txt，其中保存的是员工邮件通信的邻接列表。
:::

## 6.5.3 创建图（2/2）

- 类似地第一个边Edge (0 1 1)表示起始点为0和目标点为1之间的通信
- 代码 6‑27
- 事实证明，这些相同的节点也是19021的传入边的起始节点

::: notes
第一个节点(19021,1)的点ID为19021，并且其属性确实设置为1，类似地第一个边Edge (0,1,1)表示起始点为0和目标点为1之间的通信，边具有方向。为了表达无向图或双向图，我们可以在两个方向上链接每个点，例如在电子邮件网络中，我们可以验证19021节点同时具有传入和传出链接，首先我们收集与节点19021通信的目标节点。
:::

## 6.5.3 创建图：代码片段（1/14）

- 先对照代码理解 下面开始在 Spark 交互环境中构建三种不同类型的图
- 就能直观看到这一点
- 先过滤出真正要参与计算的数据
- 用 map 把输入转换成后续要处理的结构

片段 1
```scala
scala> import org.apache.spark.graphx._
import org.apache.spark.graphx._
scala> import org.apache.spark.rdd._
import org.apache.spark.rdd._
// ...
```

片段 2
```scala
scala> emailGraph.vertices.take(5)
res15: Array[(org.apache.spark.graphx.VertexId, Int)] =
Array((18624,1), (32196,1), (32432,1), (9166,1), (7608,1))
scala> emailGraph.edges.take(5)
// ...
```

片段 3
```scala
scala> emailGraph.edges.filter(_.srcId ==
19021).map(_.dstId).collect()
res17: Array[org.apache.spark.graphx.VertexId] = Array(696, 4232,
6811, 8315, 26007)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 6.5.3 创建图：代码片段（2/14）

- 先过滤出真正要参与计算的数据
- 用 map 把输入转换成后续要处理的结构

```scala
scala> emailGraph.edges.filter(_.dstId ==
19021).map(_.srcId).collect()
res18: Array[org.apache.spark.graphx.VertexId] = Array(696, 4232,
6811, 8315, 26007)
```

::: notes
这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 6.5.3 创建图：代码片段（3/14）

- 先对照代码理解 在一些应用里

```scala
scala> import scala.io.Source
import scala.io.Source
scala>
Source.fromFile("/data/ingr_info.tsv").getLines().take(7).foreach(println)
# id ingredient name category
0 magnolia_tripetala flower
1 calyptranthes_parriculata plant
2 chamaecyparis_pisifera_oil plant derivative
3 mackerel fish/seafood
4 mimusops_elengi_flower flower
5 hyssop herb
// ...
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.3 创建图：代码片段（4/14）

- 先对照代码理解 实际上

```scala
scala> class FNNode(val name: String) extends Serializable
defined class FNNode
scala> case class Ingredient(override val name: String, category:
String) extends FNNode(name)
defined class Ingredient
scala> case class Compound(override val name: String, cas: String)
extends FNNode(name)
defined class Compound
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.3 创建图：代码片段（5/14）

- 把原始数据从存储读入 Spark
- 先过滤出真正要参与计算的数据

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
val ingredients: RDD[(VertexId, FNNode)] =
sc.textFile("/data/ingr_info.tsv")
.filter(!_.startsWith("#"))
.map {
line =>
val row = line split '\t'
(row(0).toInt, Ingredient(row(1), row(2)))
}
// Exiting paste mode, now interpreting.
// ...
```

::: notes
这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。

这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。
:::

## 6.5.3 创建图：代码片段（6/14）

- 把原始数据从存储读入 Spark
- 先过滤出真正要参与计算的数据

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
val compounds: RDD[(VertexId, FNNode)] =
sc.textFile("/data/comp_info.tsv")
.filter(!_.startsWith("#"))
.map {
line =>
val row = line.split('\t')
(10000L + row(0).toInt, Compound(row(1), row(2)))
}
// Exiting paste mode, now interpreting.
// ...
```

::: notes
这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。

这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。
:::

## 6.5.3 创建图：代码片段（7/14）

- 把原始数据从存储读入 Spark
- 先过滤出真正要参与计算的数据

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
val links: RDD[Edge[Int]] = sc.textFile("/data/ingr_comp.tsv")
.filter(!_.startsWith("#"))
.map {
line =>
val row = line.split('\t')
Edge(row(0).toInt, 10000L + row(1).toInt, 1)
}
// Exiting paste mode, now interpreting.
links: org.apache.spark.rdd.RDD[org.apache.spark.graphx.Edge[Int]] =
MapPartitionsRDD[11] at map at \<pastie\>:32
```

::: notes
这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。

这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。
:::

## 6.5.3 创建图：代码片段（8/14）

- 先对照代码理解 解析数据集中邻接列表的行时

```scala
scala> val nodes = ingredients ++ compounds
nodes: org.apache.spark.rdd.RDD[(org.apache.spark.graphx.VertexId,
FNNode)] = UnionRDD[12] at $plus$plus at \<console\>:33
scala> val foodNetwork = Graph(nodes, links)
foodNetwork: org.apache.spark.graphx.Graph[FNNode,Int] =
<org.apache.spark.graphx.impl.GraphImpl@1435103b>
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.3 创建图：代码片段（9/14）

- 先对照代码理解 先看一下成分与化合物图 foodNetwork 的内部数据

```scala
scala> def showTriplet(t: EdgeTriplet[FNNode, Int]): String = "The
ingredient " ++ t.srcAttr.name ++ " contains " ++ t.dstAttr.name
showTriplet: (t:
org.apache.spark.graphx.EdgeTriplet[FNNode,Int])String
scala> foodNetwork.triplets.take(5).foreach(showTriplet _ andThen
println _)
The ingredient calyptranthes_parriculata contains citral_(neral)
The ingredient chamaecyparis_pisifera_oil contains undecanoic_acid
The ingredient hyssop contains myrtenyl_acetate
The ingredient hyssop contains
4-(2,6,6-trimethyl-cyclohexa-1,3-dienyl)but-2-en-4-one
The ingredient buchu contains menthol
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.3 创建图：代码片段（10/14）

- 先对照代码理解 首先
- 先对照代码理解 然后

片段 1
```scala
scala> import scala.math.abs
import scala.math.abs
scala> import breeze.linalg.SparseVector
import breeze.linalg.SparseVector
```

片段 2
```scala
scala> type Feature = breeze.linalg.SparseVector[Int]
defined type alias Feature
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.3 创建图：代码片段（11/14）

- 用 map 把输入转换成后续要处理的结构

```scala
scala> import scala.io.Source
import scala.io.Source
scala> :paste
// Entering paste mode (ctrl-D to finish)
val featureMap: Map[Long, Feature] = Source.fromFile("/data/ego.feat")
.getLines()
.map {
line =>
val row = line.split(' ')
val key = abs(row.head.hashCode.toLong)
val feat = SparseVector(row.tail.map(_.toInt))
// ...
```

::: notes
map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 6.5.3 创建图：代码片段（12/14）

- 先对照代码理解 每行中的第一个数字对应于自我中心网络中的节点ID

```text
val key = abs(row.head.hashCode.toLong)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.3 创建图：代码片段（13/14）

- 把原始数据从存储读入 Spark

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
val edges: RDD[Edge[Int]] = sc.textFile("/data/ego.edges")
.map {
line =>
val row = line split ' '
val srcId = abs(row(0).hashCode.toLong)
val dstId = abs(row(1).hashCode.toLong)
val srcFeat = featureMap(srcId)
val dstFeat = featureMap(dstId)
val numCommonFeats = srcFeat dot dstFeat
// ...
```

::: notes
这一句是在确定输入边界。讲的时候要说明数据来自哪里、现在进入了什么抽象对象，以及后面的转换是围绕哪个数据集继续推进的。
:::

## 6.5.3 创建图：代码片段（14/14）

- 先对照代码理解 最后
- 先过滤出真正要参与计算的数据

片段 1
```scala
scala> val egoNetwork: Graph[Int, Int] = Graph.fromEdges(edges, 1)
egoNetwork: org.apache.spark.graphx.Graph[Int,Int] =
org.apache.spark.graphx.impl.GraphImpl@659d6f74
```

片段 2
```scala
scala> egoNetwork.edges.filter(_.attr == 3).count()
res2: Long = 1852
scala> egoNetwork.edges.filter(_.attr == 2).count()
res3: Long = 9353
scala> egoNetwork.edges.filter(_.attr == 1).count()
res4: Long = 107934
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。
:::

## 6.5.3 创建图：图示

![](../../md/media/06_graph_processing/media/image9.png){width=72%}

::: notes
先快速看一下 ego.feat 文件，就能理解前面那串 RDD 转换到底在处理什么。ego.feat 的每一行都具有下面这样的结构。
:::

## 6.5.4 探索图（1/2）

- 我们将探索这三个图，并介绍网络节点的重要属性，即节点的度
- 代码 6‑43
- 在此示例中员工的入度和出度完全相同
- 代码 6‑44

::: notes
现在，我们将探索这三个图，并介绍网络节点的重要属性，即节点的度。节点的度代表其与其他节点的链接数。在有向图中，我们可以区分节点的传入度或入度（即其传入链接的数量）与节点的传出度或出度（即其传出链接的数量），我们将探讨三个示例网络的度分布。

实际上，在此示例中员工的入度和出度完全相同，因为电子邮件网络图是双向的，可以通过查看平均度来确认。
:::

## 6.5.4 探索图（2/2）

- 如果我们想找到发送电子邮件给最多人的人，则可以定义并使用以下max函数
- 代码 6‑45
- 输出结果
- 代码 6‑46

::: notes
这一页会把 如果我们想找到发送电子邮件给最多人的人，则可以定义并使用以下max函数、代码 6‑45 和 输出结果 串成一条完整流程。讲的时候重点说明每一步接在前一步后面，分别解决了什么问题。
:::

## 6.5.4 探索图：代码片段（1/6）

- 先对照代码理解 现在
- 用 map 把输入转换成后续要处理的结构

片段 1
```scala
scala> emailGraph.numEdges
res15: Long = 367662
scala> emailGraph.numVertices
res16: Long = 36692
```

片段 2
```scala
scala> emailGraph.inDegrees.map(_._2).sum / emailGraph.numVertices
res17: Double = 10.020222391802028
scala> emailGraph.outDegrees.map(_._2).sum / emailGraph.numVertices
res18: Double = 10.020222391802028
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。
:::

## 6.5.4 探索图：代码片段（2/6）

- 先对照代码理解 如果我们想找到发送电子邮件给最多人的人

```scala
scala> :paste
// Entering paste mode (ctrl-D to finish)
def max(a: (VertexId, Int), b: (VertexId, Int)): (VertexId, Int) = {
if (a._2 > b._2) a else b
}
// Exiting paste mode, now interpreting.
max: (a: (org.apache.spark.graphx.VertexId, Int), b:
(org.apache.spark.graphx.VertexId,
Int))(org.apache.spark.graphx.VertexId, Int)
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.4 探索图：代码片段（3/6）

- 先对照代码理解 先看输出结果： 的执行步骤
- 先过滤出真正要参与计算的数据

片段 1
```scala
scala> emailGraph.outDegrees.reduce(max)
res19: (org.apache.spark.graphx.VertexId, Int) = (5038,1383)
```

片段 2
```scala
scala> emailGraph.outDegrees.filter(_._2 <= 1).count
res20: Long = 11211
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。

这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。
:::

## 6.5.4 探索图：代码片段（4/6）

- 先过滤出真正要参与计算的数据

```scala
scala> foodNetwork.outDegrees.reduce(max)
res21: (org.apache.spark.graphx.VertexId, Int) = (908,239)
scala> foodNetwork.vertices.filter(_._1 == 908).collect()
res22: Array[(org.apache.spark.graphx.VertexId, FNNode)] =
Array((908,Ingredient(black_tea,plant derivative)))
scala> foodNetwork.inDegrees.reduce(max)
res23: (org.apache.spark.graphx.VertexId, Int) = (10292,299)
scala> foodNetwork.vertices.filter(_._1 == 10292).collect()
res24: Array[(org.apache.spark.graphx.VertexId, FNNode)] =
Array((10292,Compound(1-octanol,111-87-5)))
```

::: notes
这一步是在缩小计算范围。把过滤条件讲清楚，后面聚合出来的结果才能知道是在什么约束下得到的。
:::

## 6.5.4 探索图：代码片段（5/6）

- 我们也可以继续计算自我网络中的连接度

```scala
scala> egoNetwork.degrees.reduce(max)
res25: (org.apache.spark.graphx.VertexId, Int) = (1643293729,1084)
scala> :paste
// Entering paste mode (ctrl-D to finish)
def min(a: (VertexId, Int), b: (VertexId, Int)): (VertexId, Int) = {
if (a._2 < b._2) a else b
}
// Exiting paste mode, now interpreting.
min: (a: (org.apache.spark.graphx.VertexId, Int), b:
(org.apache.spark.graphx.VertexId,
Int))(org.apache.spark.graphx.VertexId, Int)
// ...
```

::: notes
讲这段代码时，先指出输入对象，再说明中间做了哪些转换，最后在哪里触发执行或写出结果。这样听众能把 API 调用和完整处理链对上。
:::

## 6.5.4 探索图：代码片段（6/6）

- 用 map 把输入转换成后续要处理的结构
- 通过 action 触发前面的惰性计算真正执行

```scala
scala> egoNetwork.degrees.map(t => (t._2, t._1)).groupByKey.map(t
=> (t._1, t._2.size)).sortBy(_._1).collect()
res27: Array[(Int, Int)] = Array((1,15), (2,19), (3,12), (4,17),
(5,11), (6,19), (7,14), (8,9), (9,8), (10,10), (11,1), (12,9), (13,6),
(14,7), (15,8), (16,6), (17,5), (18,5), (19,7), (20,6), (21,8), (22,5),
(23,8), (24,1), (25,2), (26,5), (27,8), (28,4), (29,6), (30,7), (31,5),
(32,10), (33,6), (34,10), (35,5), (36,9), (37,7), (38,8), (39,5),
(40,4), (41,3), (42,1), (43,3), (44,5), (45,7), (46,6), (47,3), (48,6),
(49,1), (50,9), (51,5), (52,8), (53,8), (54,4), (55,2), (56,5), (57,7),
(58,4), (59,8), (60,9), (61,12), (62,5), (63,15), (64,5), (65,7),
(66,6), (67,9), (68,4), (69,5), (70,4), (71,7), (72,9), (73,10), (74,2),
// ...
```

::: notes
map 的作用是保持数据条数大体不变，但把每条记录改造成更适合后续计算的形式。讲的时候要点出转换前后的结构差异。

前面的转换大多只是把执行计划串起来，直到这里遇到 action，Spark 才会真正下发任务。这个时点是理解执行模型的关键。
:::

